# 프로젝트: 요리 레시피 & 재료 관리 앱 (CookKit)

## 배경
- 밀가루·마늘 알러지가 있는 친구/가족과 함께 요리할 일이 많아서 시작한 개인 프로젝트
- 처음엔 Claude와 대화하며 알러지 없는 리조또 레시피를 짜고, 인분 조절 + 장보기 리스트 정리를 반복하다가
  "아예 앱으로 만들자"는 결론에 도달함
- 개발자: 회사에서 짬짬이 작업 중 (오늘은 회사 PC, 이후 집 PC에서 이어서 작업). Git으로 코드는 그대로 이어받지만
  Claude Code 대화 세션 자체는 기기별로 로컬 저장이라 이어지지 않음 → 이 파일이 맥락 전달용

## 문서 구성
이 파일은 항상 로드되는 "안내데스크"만 담고, 상세 아카이브는 `docs/`에 따로 둡니다.
- `docs/architecture.md` — 주제별 아키텍처/기술 결정 노트(왜 이렇게 만들었는지, 버그 원인 등).
  관련 기능을 다룰 때 직접 Read할 것.
- `docs/changelog-archive.md` — 2~21차 확장의 상세 이력(아래 "진행 상황"엔 회차 제목만 남김).
- 아래 "진행 상황"의 최근 회차(22차~)는 아직 실사용 검증이 덜 끝났거나 참조가 잦을 가능성이 높아
  이 파일에 그대로 남겨둠 — 시간이 지나 안정되면 다음에 아카이브로 옮길 것.

## 진행 상황
(2~21차의 자세한 내용은 `docs/changelog-archive.md` 참고 — 아래는 회차 제목만)
- **2차 확장 완료**: 로그인 화면, 레시피 중심 네비게이션, 유튜브 링크 변환, 재료 카테고리 동적화
- **3차 확장 완료**: Google Gemini를 두 번째 AI 제공자로 추가(무료 쿼터 대안), 레시피 추가 화면 개선
- **4차 확장 완료**: 유튜브 자막 자동 추출(서버리스 함수 도입)
- **5차 확장 완료**: "따뜻한 아날로그 + Soft UI" 디자인 통일 + 다크모드, 재료별 개인 선호 설정, AI 이미지 생성
- **6차 확장 완료 — Supabase(DB) 전환**
- **7차 확장 완료 — 레시피 관리 화면 모바일 개편**
- **8차 확장 완료 — 카테고리 중복 버그 수정 + 태그 국가/스타일 축 + 난이도/조리시간 자동 판단**
- **9차 확장 완료 — 이미지 저장 Supabase Storage 전환 + 완성 사진**
- **10차 확장 완료 — 다른 가구 공개 레시피 둘러보기/복사 + 레시피 공개범위 3단계**
- **11차 확장 완료 — 유튜브 변환 시 완성 사진 자동 제안**
- **12차 확장 완료 — 레시피 작성자 표시 + 프로필 닉네임/사진 + household 이름 짓기 가이드**
- **13차 확장 완료 — API 키 Vault 전환(DB 전환 Phase 4)**
- **14차 확장 완료 — 재료 유통기한 관리 + PWA/웹 푸시 알림**
- **15차 확장 완료 — 공공데이터 기반 대량 레시피 시딩(200개)**
- **16차 확장 완료 — 레시피 목록 넷플릭스식 가로 스크롤 행 구조**
- **17차 확장 완료 — 영수증 스캔 재료 등록 + 요리 완료 기록/재료 자동 차감**
- **18차 확장 완료 — 요리 모드(핸즈프리 안내 + 음성 명령)**
- **19차 확장 완료 — 요리 모드 실제 소요시간 기록 + 복합 요리(여러 레시피 동시 진행)**
- **20차 확장 완료 — 실사용 피드백 12건 반영(버그 3 + 발견성 1 + 기능 5 + UI 2)**
- **21차 확장 완료 — 재료 관리 화면 "냉장고" 컨셉 개편(DIS-06)**
- **22차 확장 완료 — 디자인 시스템 전면 교체(Claude Design 핸드오프)**: Claude Design으로
  만든 새 디자인(`CookKit mobile app prototype/design_handoff_cookkit_home_plan/`, organic
  디자인 시스템 — 크림 배경 + 테라코타/세이지 팔레트, 제목 Jua/본문 Gowun Dodum)을 홈 화면
  추가 정도가 아니라 **앱 전체 디자인 시스템 교체**로 적용하는 작업. 1단계(토큰 교체)는 사용자
  확인까지 마쳤고, 2~5단계(탭 구조/홈 화면/프로필 진입/주간 일정 화면)까지 전부 완료.
  - **토큰**: `src/index.css`의 색상/폰트/radius/그림자/간격 값을 전부 교체(변수 이름은
    `--bg-page`/`--accent`/`--border` 등 기존 이름 유지, 값만 교체 — 이름을 바꾸면 참조하는
    곳을 전부 고쳐야 해서). `--danger`는 핸드오프 팔레트에 없는 역할이라 새로 설계(#a8382a
    라이트 / #e2685a 다크), `--success`는 세이지(accent-2-600), `--warning`은 앰버색이 없어
    테라코타 진한 톤(accent-700)을 재사용. 다크모드는 핸드오프에 값이 없어 직접 설계 — 단순
    반전 대신 핸드오프 자체의 neutral-900(따뜻한 다크브라운)을 재사용하고, accent/accent-2는
    라이트보다 밝은 램프 단계로, 테두리는 밝은색 저투명도 hairline으로 처리(기존 `.card` 등에
    이미 `border: 1px solid var(--border)`가 있어서 구조 변경 없이 값만 바꿔도 적용됨), 본문
    텍스트 대비는 전부 WCAG AA(4.5:1) 이상 확보. 버튼은 핸드오프의 "small controls go pill"
    규칙대로 전부 pill(999px)로 바꿨지만, 여러 줄 textarea/좁은 숫자 입력칸까지 pill로 만들면
    어색해서 `.field input`/`select`는 카드 radius(`--radius-sm`, 8px)로 남김(핸드오프와의
    의도적 차이). 폰트는 이번 단계에서 패밀리 교체(Jua/Gowun Dodum) + 화면 제목(`h1`) 28px만
    반영하고, 카드 제목/섹션 제목 등 세부 크기 재조정은 실제 그 컴포넌트를 만들 때(2~5단계)
    화면별로 맞추기로 하고 미룸(한 번에 다 바꾸면 기존 그리드/칩 레이아웃이 깨질 위험).
  - **탭 구조**: 레시피/장보기/냉장고/설정(4탭) → **홈/레시피/냉장고/장보기**(4탭)로 교체,
    설정 탭은 삭제하고 홈 우상단 프로필 아이콘 → 바텀시트로 이동. 핸드오프는 일정 탭을 포함한
    5탭을 제안했지만, 식단 계획은 사용 빈도가 낮다고 판단해 탭에 넣지 않고 홈 화면 안의
    "이번 주 일정" 스트립 → push 방식으로 진입하는 구조로 축소(`src/data/activeTab.ts`의
    `Tab` 타입, 기본값 `'home'`).
  - **"설정으로 이동" 신호 체계**: 설정 탭이 없어지면서, 앱 곳곳의 "API 키가 없어요, 설정으로
    이동" 버튼들(`ReceiptScanModal.tsx`/`RecipeChatPanel.tsx`/`RecipeEditor.tsx`)이 갈 곳이
    없어짐 — `src/data/profileSheet.ts`(activeTab.ts와 같은 `useSyncExternalStore` 패턴)를
    추가해서 `requestProfileSheet()`가 홈 탭으로 이동시키고 "바텀시트를 열어달라"는 신호를
    `HomePage`에 전달, `HomePage`가 그 신호를 받아 `ProfileSheet`를 연다. 홈 검색 필드도 같은
    패턴(`src/data/recipeSearchFocus.ts`)으로 레시피 탭으로 이동시킨다(단, 실제 검색창 자동
    포커스까지는 아직 안 붙임 — 탭 이동만 확인됨).
  - **프로필 바텀시트**(`src/features/settings/ProfileSheet.tsx`, 신규): 예전
    `SettingsPage.tsx`(삭제됨)의 내용을 내 프로필/가구 설정/앱 설정/태그·카테고리 관리 4개
    메뉴로 재분류 + 로그아웃은 메뉴 목록에 바로 노출. 각 섹션은 기존 로직을 거의 그대로
    재사용(`InlineEditRow`도 이 파일로 이동), "태그·카테고리 관리"는 새로 만들지 않고 기존
    `TagManager`/`CategoryManager` 모달을 그대로 여는 버튼 2개로 구성(레시피 탭/냉장고 탭의
    기존 진입점은 그대로 남겨둠 — 두 곳 다 유효).
  - **홈 화면**(`src/features/home/HomePage.tsx`, 신규): 인사 행 + 프로필 아바타(→ 바텀시트),
    검색 필드(→ 레시피 탭), 냉장고 보유 재료 칩(→ 냉장고 탭), 오늘의 추천 카드(보유 재료
    매칭률이 가장 높은 레시피 하나 — `src/lib/recipeRecommendation.ts`의 `pickTodayRecommendation`,
    복잡한 알고리즘 없이 단순 비율 비교), 이번 주 일정 스트립, 유통기한 임박 재료 목록(각 행에
    그 재료를 쓰는 레시피로 바로 가는 "레시피" 배지 —
    `pickBestRecipeUsingIngredient`). 홈은 자체적으로 로컬 push 스택을 가지고 있어서
    (`RecipesFeature.tsx`와 같은 패턴) 추천 카드나 유통기한 배지를 누르면 탭 전환 없이 홈 위에
    `RecipeDetailPage`/`RecipeEditor`가 그대로 뜨고, 뒤로가기하면 다시 홈으로 돌아온다(핸드오프
    "상세의 뒤로 버튼은 직전 탭으로 복귀" 요구사항 그대로).
  - **주간 일정 데이터 모델**(`0022_meal_plans.sql`): `meal_plans(id, household_id, date,
    recipe_id)` — household 공유, 지금은 저녁 한 끼만 관리(`(household_id, date)` 유니크,
    나중에 `meal_type`을 추가할 수 있게 여지만 남김). 조회(`fetchMealPlans`)/쓰기
    (`setMealPlan`/`clearMealPlan`)는 `src/data/mealPlans.ts`.
  - **주간 일정 화면**(`src/features/home/WeeklyPlanPage.tsx`, 신규, 홈에서 push로 진입 —
    탭 아님, 뒤로가기로 홈 복귀): 요일 스트립(월~일, 계획 있으면 점 표시) → 선택된 날 카드
    (레시피 있으면 이미지+제목+메타+"레시피 보기"/"바꾸기", 없으면 "아직 정하지 않았어요"+
    "메뉴 정하기") → 이어 쓰기 박스 → 이 날 살 것(체크하면 `usePantryStatus().setOwned`로
    바로 보유 처리 — 21차 확장의 `markIngredientFilled` 채움 이력에도 반영됨). 레시피
    배치(`MealPlanRecipePicker`, 이 파일 내부 컴포넌트)는 검색 가능한 목록에서 한 번 탭하면
    바로 저장되는 원탭 방식(요구사항대로 — 확인 단계를 넣지 않음).
    - **"이어 쓰기" 로직은 명세보다 단순화함**: 명세 예시("남은 우유 200ml를 이 날 다 쓰게
      됩니다")는 재료의 "남은 수량"을 추적한다는 뜻인데, 이 앱의 데이터 모델은 재료를
      boolean(`owned`)으로만 관리해서 실제 잔량 계산이 불가능함. 대신 **선택된 날의 레시피와
      바로 앞/뒤 요일 레시피가 재료를 공유하는지**만 감지해서 안내(`sharedIngredientNames`) —
      겹치는 재료 중 유통기한이 임박/경과한 게 있으면 그 배지를 우선 표시, 없으면 겹치는 재료
      이름을 나열, 아예 안 겹치면 "이 날 재료는 앞뒤 요일과 겹치지 않아요."(명세 문구 그대로).
    - **훅 순서 버그(작성 중 자체 발견 및 수정)**: 처음 작성할 때 `useStoredImage(coverImageId)`
      호출을 `if (view.screen === 'detail') return ...` 조건부 return **아래**에 뒀었음 —
      레시피 상세로 push했다가 뒤로 나오면(`view.screen`이 바뀌면) 이 훅 호출 여부 자체가
      매 렌더 달라져서 React "Rules of Hooks" 위반(훅 호출 순서는 항상 동일해야 함). 빌드는
      통과했지만(타입 에러가 아니라 런타임 규칙이라 tsc가 못 잡음) 코드 리뷰 중 발견해서 모든
      훅 호출을 조건부 return보다 위로 옮김 — `HomePage.tsx`는 처음부터 올바른 순서였음(같은
      패턴을 새로 짤 때마다 훅이 조건부 return보다 위에 있는지 확인할 것).
  - **탭바**: lucide-react 아이콘 도입(이 프로젝트 첫 사용 — house/book-open/refrigerator/
    shopping-cart, stroke-width 2.75), 높이 96px로 확대(핸드오프 규격), 장보기 탭에 "아직
    안 산 재료 개수" 배지 추가(`useShoppingNeededCount`, `src/data/shoppingSelection.ts`).
  - **실기기/수동 테스트 필요**: 로그인 화면(라이트/다크)만 Playwright로 직접 스크린샷
    확인함(콘솔 에러 없음, 폰트/색상 정상 렌더링 확인) — 로그인 이후 화면(홈/탭바/프로필
    바텀시트/주간 일정)은 실제 계정이 필요해서 검증하지 못함. 실사용 중이던 테스트 계정
    (`shinsudong+@naver.com`)으로 시도했으나 이 프로젝트가 "이메일 인증 확인"을 요구해서
    "Email not confirmed" 에러로 로그인 실패 — 사용자 판단으로 이 부분은 검증 없이 다음
    단계로 진행하기로 함. **다음에 실사용 테스트할 때 특히 확인이 필요한 것**: 홈 화면 전체
    레이아웃/다크모드 대비, 주간 일정 화면의 요일 스트립·이어 쓰기 문구·원탭 레시피 배치,
    프로필 바텀시트 각 섹션, 탭바 아이콘/배지, 홈 검색 필드(레시피 탭으로 이동은 하지만
    검색창 자동 포커스는 아직 안 붙어있음 — 필요하면 추가 요청할 것).
- **23차 확장 완료 — 홈 동적 인사말 + 알림 기능 + 영양 정보**: A(인사말) → B(알림) → C(영양) 순서로
  진행.
  - **A. 동적 인사말**(`src/lib/homeGreeting.ts`): AI 호출 없이 전부 규칙 기반. 우선순위 —
    (1) 오늘 이미 요리 기록이 있으면(household 공유, `fetchTodayCookingLog`) 그 문구 풀에서
    선택 (2) 오늘 주간 일정에 레시피가 배치돼 있으면 "오늘은 OO예요" (3) 유통기한 임박(3일
    이내)만 (soon 4~7일 제외) 재료가 있으면 (4) 장보기에 담긴 게 있으면 (5) 그 외엔 시간대
    (아침~10시/점심~17시/저녁17시~)·주말 여부 기반 기본 문구 풀. 기본 문구는 "날짜+시간대"
    문자열을 해시해서 안정적 인덱스를 뽑는 방식으로 "같은 날엔 유지, 시간대 바뀌면 전환"을
    별도 저장 없이 구현. "의미 있는 행동 직후 즉시 반영"은 홈 탭으로 돌아올 때
    (`useActiveTab()`)와 홈 안에서 화면을 오갔다 돌아올 때(`view.screen`) 둘 다를 트리거로
    오늘 요리 여부를 다시 조회하는 방식으로 처리(다른 탭에서 요리를 완료한 경우까지 커버).
    그 위 줄("수요일 저녁 · OO님")은 시간대에 따라 아침/점심/저녁만 바뀜.
  - **B. 알림**(`notifications` 테이블, 0023 마이그레이션): `user_id`(받는 사람)가 실제
    행위자(auth.uid())와 달라 일반 RLS insert 정책으로 표현이 안 되고 임의 개방은 스팸
    벡터가 되므로, `create_recipe_liked_notification`/`delete_recipe_liked_notification`/
    `create_household_recipe_added_notifications` 3개 SECURITY DEFINER RPC로만 생성/삭제하고
    테이블 자체엔 INSERT/DELETE RLS 정책을 아예 두지 않음(SELECT/UPDATE만 본인 것 허용) — 각
    RPC가 "레시피를 정말 그 사람이 소유하는지"/"정말 같은 household인지"/"본인 알림이
    아닌지"를 서버에서 검증. 공개 레시피 좋아요 토글 시(`PublicRecipeDetailPage.tsx`) 알림
    생성/삭제, 새 레시피 저장 시(`RecipeEditor.tsx`, **수정은 제외 — 신규 생성일 때만** +
    `visibility!=='private'`일 때만, private면 다른 가구원이 애초에 못 여는 레시피라 알림이
    의미 없음) household 구성원에게 알림 생성 — 둘 다 알림 처리 실패가 원래 동작(좋아요/저장)
    자체를 막지 않도록 별도로 감쌈. 프로필 바텀시트 최상단에 "알림" 메뉴(안 읽은 개수 배지),
    목록은 시간순 + 안 읽은 것 배경 강조, 탭하면 그 알림만 읽음 처리(전체 읽음 처리 안 함 —
    한꺼번에 지워지면 뭐가 있었는지 확인 못 함) 후 관련 레시피 상세로 이동. 홈 화면 프로필
    아바타에 안 읽은 알림 있으면 빨간 점. **범위에서 뺀 것**: 명세의 B-4(인사말 아래 알림
    힌트 줄)는 "선택" 표시 항목이라 이번엔 건너뜀.
  - **C. 영양 정보**: 데이터 모델은 `Recipe.nutrition`(1인분 기준 calories/carbs/protein/
    fat/sodium) + `Recipe.nutritionSource`('public_data'|'api'|'ai_estimate'|'manual'),
    다른 중첩 데이터처럼 `recipes.content` jsonb 안에 저장.
    - **식약처 영양성분 API(C-2) 매칭 테스트 — 결론: 이번엔 보류**. 사용자가 data.go.kr에서
      "식품의약품안전처_식품영양성분DB정보"(`FoodNtrCpntDbInfo02`,
      `getFoodNtrCpntDbInq02`, 검색 파라미터 `FOOD_NM_KR`)를 신규로 활용신청해서 연동
      테스트까지 진행함 — API 자체는 정상 응답(약 30만 건). 그런데 실제 재료명(양파/두부/
      대파/스파게티/마늘/설탕/간장/계란/돼지고기/소고기/감자/당근, 12개)으로 검색해본 결과
      **정확히 일치하는 항목이 0/12건**이었음 — 이 DB는 "양파볶음"/"된장찌개_두부"처럼
      **완성된 요리·상용제품 위주**로 구성돼 있고 순수 재료(생 양파, 생 두부 등) 단독
      항목이 없어서, "재료명 → 영양성분 조회 → 레시피 전체 합산" 용도로는 안 맞음(참고: 이건
      식약처가 제공하는 여러 영양 DB 중 하나일 뿐이고, raw 재료 단위 성분표는 농촌진흥청
      국가표준식품성분표(koreanfood.rda.go.kr)가 더 적합해 보이나 별도 기관/API라 이번
      범위에서는 시도하지 않음). 사용자가 사전에 "성공률 낮으면 API는 건너뛰고 공공데이터+AI
      추정만 쓰자"고 합의해둔 대로 C-2는 스킵하고 C-1+C-3만 구현.
    - **C-1 백필**(`scripts/backfill-nutrition-from-public-data.ts`, 실행 완료): 15차 확장
      때 COOKRCP01에서 시딩한 공공데이터 레시피 412건은 원래 시딩 스크립트가 API 응답의
      `INFO_ENG`(열량)/`INFO_CAR`(탄수화물)/`INFO_PRO`(단백질)/`INFO_FAT`(지방)/`INFO_NA`
      (나트륨) 필드를 받아오고도 저장하지 않고 버렸던 것을 뒤늦게 채워 넣는 1회성 스크립트 —
      COOKRCP01은 RCP_SEQ 단건 조회를 지원하지 않아(테스트로 확인 — 파라미터를 붙여도
      무시됨) 전체 데이터셋(당시 1,156건)을 페이지로 훑어 RCP_SEQ→영양정보 맵을 만든 뒤
      매칭. **412/412건 전부 매칭 성공**(재실행해도 안전 — `content.nutrition` 있으면
      건너뜀). `nutritionSource: 'public_data'`로 표시.
    - **C-3 AI 온디맨드 추정**: `claudeClient.ts`/`geminiClient.ts`의
      `estimateRecipeNutrition`(레시피 이름+재료 목록을 근거로 1인분 기준 추정, 구조화 JSON
      출력) + `api/ai-nutrition.ts`(ai-chat.ts와 같은 두 제공자 분기 패턴) +
      `aiProxy.ts`의 `estimateRecipeNutrition`. 레시피 상세 화면에 "영양 정보 계산하기"
      버튼(자동 호출 없음, 사용자가 눌렀을 때만) — 계산되면 `nutritionSource: 'ai_estimate'`
      로 저장되고 화면에 "AI 추정 · 실제와 다를 수 있어요" 문구가 항상 같이 표시됨(정확한
      척하지 않기 위함).
    - **표시 위치**: 레시피 상세(1인분 기준, 칼로리 강조+탄단지·나트륨 칩, 인분 조절
      스테퍼와 같이 있어서 `servings` 바뀌면 즉시 재계산 — `nutrition` 필드 자체가 이미
      "1인분 기준"으로 저장돼 있어 `scaleAmount`가 아니라 단순히 `값 × servings`로 계산,
      재료 수량 스케일링(`scaleAmount(amount, servingsBase, servings)`)과는 다른 공식이라
      혼동 주의) / 레시피 카드·리스트 항목(칼로리만, "2인분 · 20분 · 520kcal") / 주간 일정
      선택된 날 카드("이 날 합계 약 Nkcal" = 1인분 값 × `servingsBase`, 그 레시피 전체 분량
      기준). `RecipeEditor.tsx`는 영양 정보를 편집하는 UI가 없어서 저장 시 `existing?.
      nutrition`/`nutritionSource`를 그대로 들고 다녀 재료/조리순서 수정 저장 때 지워지지
      않게 함(sourceRecipeId와 같은 패스스루 패턴).
  - **버그(수정 완료) — 서버리스 함수가 브라우저 전용 모듈을 물고 있던 문제**: 이번 작업
    도중 발견 — 지난 보안 리뷰(SECURITY_REVIEW_FIXES.md, P0-1)에서 `youtube-transcript`
    엔드포인트에 인증을 추가하면서 `src/lib/youtubeTranscript.ts`가 `aiProxy.ts`의
    `getAuthHeader`를 정적 import하게 됐는데, `aiProxy.ts`는 브라우저의 Supabase 세션에
    의존하고 `supabaseClient.ts`(`import.meta.env.VITE_*`, Vite 전용 문법)를 물고 있는
    클라이언트 전용 모듈. `youtubeTranscript.ts`의 `extractYoutubeVideoId`는
    `geminiClient.ts`를 거쳐 `api/ai-chat.ts` 등 여러 서버리스 함수에서도 쓰이는데, 정적
    import는 모듈 로드 시점에 그 코드가 같이 평가되므로 — `fetchYoutubeTranscript`를 실제로
    호출하지 않아도 — Vite의 `import.meta.env` 치환이 없는 서버 번들 환경에서 즉시 에러가
    날 수 있었음(로컬 `npm run build`는 `tsc -b`가 `api/`를 대상에 안 둬서 못 잡음). 이번
    세션에서 `api/*.ts`를 nodenext 설정으로 단독 타입체크하다가 발견 — `getAuthHeader`
    import를 `fetchYoutubeTranscript` 함수 안의 동적 import(`await import('./aiProxy.js')`)
    로 옮겨서 실제로 그 함수가 호출될 때(항상 브라우저에서만)만 평가되게 수정. 앞으로 `src/lib/`
    아래 서버/클라이언트 공용 파일에서 다른 모듈을 새로 import할 때는 그 모듈이 브라우저 전용
    상태(세션, localStorage 등)에 의존하지 않는지 확인할 것 — 의존한다면 정적 import 대신
    실제 사용 시점의 동적 import로 분리.
- **24차 확장 완료 — 홈/레시피/냉장고/장보기 4탭 전면 개편**: 사용자가 첨부한 냉장고/홈
  시안을 참고해 C(냉장고) → D(요리↔냉장고 연결) → E(장보기) → A(홈) → B(레시피) 순서로 진행
  (C의 정리 모드를 D/A에서 재사용하기 위해 먼저 구현). 각 단계마다 별도 커밋.
  - **C. 냉장고**: 목록을 "있어요/없어요" 두 섹션 대신 **owned=true 재료만** 카테고리별 칩으로
    보여주도록 개편(칩 = 이름+`defaultBuyUnit`+유통기한 배지, 탭하면 상세 모달). 상세 모달
    (`IngredientDetailModal`)에 삭제 버튼을 내장해서 행별 토글/삭제 버튼을 없앰. 상단에
    "재료 N개 · 이걸로 만들 수 있는 레시피 M개" 요약 + 그 레시피 개수가 0보다 크면 초록
    배너("🧺 지금 재료로 바로 만들 수 있는 레시피 N개") — 탭하면 레시피 탭에 "보유 재료로
    가능" 필터가 적용된 채로 이동한다(`src/data/pantryFilterRequest.ts`, profileSheet.ts와
    같은 신호 패턴). 이 판단 기준(`isRecipeMakeableWithPantry`)은 `src/data/computed.ts`로
    뽑아서 냉장고/레시피 화면이 공유(예전엔 RecipesPage에만 로컬 함수로 있었음).
    - **"🧹 정리하기"**(`PantryTidyModal.tsx`, 신규, 재사용 컴포넌트): 모든 재료(owned 무관)를
      "있어요"/"없어요" 두 박스로 나눠 보여주고 **칩을 탭하면 반대편으로 즉시 이동**(드래그
      대신 — 복합 요리 순서 재정렬 때 "모바일 터치에서 드래그가 불안정하다"고 이미 판단해
      ▲/▼를 쓴 전례와 같은 이유, 재료가 십여 개면 드래그가 더 번거로움). **"적용"을 눌러야만**
      원래 owned 값과 달라진 재료만 골라 일괄 저장(로컬 상태만 바뀌다가 적용 시점에 반영,
      owned:true로 바뀐 건 `markIngredientFilled`로 채움 이력도 같이 남김). 이 모달은 D-1(요리
      완료 직후)과 A-4(홈 안내)에서도 그대로 재사용된다.
    - 카테고리 관리(`CategoryManager.tsx`) 입력창을 홈 검색 필드 스타일(pill, `--bg-card`,
      `--shadow-soft`)로 다듬음 — 재사용 가능한 `.pill-input-row` CSS 클래스로 뽑아둠(장보기
      "직접 추가" 등에서도 재사용 가능).
  - **D. 요리 → 냉장고 정리 연결**: `cooking_log`에 `pantry_cleaned_at` 컬럼 추가(0024
    마이그레이션) — null이면 아직 정리 안 함. 정리는 요리한 사람이 아닌 다른 가구원이 할 수도
    있어서(household 공유 작업) 기존 `cooking_log_update_own`(본인 것만) RLS로는 부족해,
    `mark_pantry_cleaned(cooking_log_id)` SECURITY DEFINER RPC로 "같은 household 멤버면"
    갱신을 허용(`src/data/cookingLog.ts`). `logCooking()`이 이제 생성된 기록의 id를
    반환하도록 바뀜(정리 모드 진입 시 이 id로 나중에 정리 완료를 표시하기 위함).
    "오늘 만들었어요" 완료 화면(`CookingLogModal.tsx`)에 "냉장고를 정리할까요?" 안내 +
    "정리하기"/"나중에" 추가 — "정리하기"를 누르면 PantryTidyModal로 이동하는데, 방금 요리에
    쓴 재료는 이미 확정 단계에서 `owned:false`로 반영된 뒤라 **별도 사전 이동 처리 없이도
    자연스럽게 "없어요" 박스에서 시작**한다(확인만 하고 남은 것만 되돌리면 되는 구조 — 명세의
    "방금 요리에 쓴 재료가 미리 없어요 쪽으로 옮겨진 상태" 요구사항이 기존 흐름만으로 저절로
    만족됨).
  - **E. 장보기**: "영수증으로 재료 업데이트" 버튼 제거(냉장고 탭에 이미 있어 중복). 완전히
    비었을 때(선택된 레시피도 직접 추가 항목도 없음) "아직 담은 게 없어요" + "레시피에서
    담기"(모달)/"직접 추가" 버튼으로 개편 — 항상 펼쳐져 있던 레시피 선택 칩 목록을 모달
    (`RecipeSelectModal`)로 옮김. 항목이 있으면 "살 것 n개 · 담은 것 m개" 요약 + 우상단
    **+ 아이콘**(레시피에서 담기/직접 추가 선택 시트) — 각 항목은 원형 체크 + 재료명 + 출처
    (레시피 이름들 또는 "직접 추가", 작게) + 수량, 체크하면 opacity 낮추고 취소선.
    - **`shopping_extra_items` 테이블(신규, 0025)**: household 공유, 레시피를 거치지 않고
      장보기에서 "직접 추가"한 항목을 추적(재료 하나당 household에 한 행, unique). 기존
      `shopping_selection`은 `recipe_id`만 다뤄서 이 용도로 못 씀 — 이 앱의 장보기가 원래
      "레시피 선택 → 재료 자동 집계" 구조였는데(21차 확장에서 이미 이 이유로 재고 선제 제안
      기능을 "장보기에 담기" 대신 "채웠어요"로 설계한 전례가 있음), 이번엔 명세가 명확히
      "직접 추가" 항목도 체크리스트/유통기한 입력 대상으로 요구해서 최소한의 새 테이블을
      추가하는 쪽으로 판단. `AddIngredientModal`(원래 냉장고 전용, `IngredientsPage.tsx`)을
      export하고 `defaultOwned` prop을 추가해(기본 true) 장보기의 "직접 추가"에서는
      `defaultOwned={false}`로 재사용.
    - **"담은 재료 냉장고로 옮기기"(E-4, 신규)**: 체크한 항목이 하나라도 있으면 하단에 버튼 →
      확인 화면에서 재료별 유통기한을 선택 입력(장 보고 온 직후가 입력하기 가장 좋은 타이밍) →
      적용하면 `markIngredientFilled`로 owned:true+lastFilledAt 갱신을 한 번에 처리하고, 직접
      추가 항목이면 `shopping_extra_items`에서 제거(레시피 유래 항목은 owned:true가 되면
      "구매 필요" 필터에서 자연히 빠지므로 별도 삭제 불필요). 영수증 스캔 흐름에는 이번에
      유통기한 입력을 추가하지 않음(범위를 좁게 유지하기 위한 판단 — 필요해지면 다음에 추가).
  - **A. 홈**: "오늘의 추천" 카드 아래에 **"⏱ 20분 안에 되는 것"** 가로 스크롤 섹션 추가
    (`estimatedMinutes<=20`인 레시피가 **3개 미만이면 섹션 자체를 숨김**, 카드 = 이미지+이름+
    "12분 · 재료 5개"). "전체 보기"는 새 `src/data/recipeTimeFilterRequest.ts` 신호로 레시피
    탭에 조리시간 필터가 적용된 채로 이동(B에서 만든 필터 바텀시트의 `maxCookMinutes` 상태를
    그대로 프리셋). **"이번 달 N번 요리했어요"** 요약 카드(신규 `fetchMonthlyCookingCount`) —
    탭하면 홈 안에서 `CookingHistoryPage`로 push(레시피 탭 것과 별개 진입점, 컴포넌트는 그대로
    재사용). 추천 카드를 클릭 영역(상세로 이동)과 **"🍳 바로 요리하기"** 버튼으로 분리(버튼
    안에 버튼을 중첩할 수 없어서 구조 변경) — `RecipeDetailPage`에 `autoStartCookingMode`
    prop을 추가해 상세 화면을 거치지 않고 확인 다이얼로그 없이 곧바로 요리 모드가 열리게 함.
    **"냉장고 정리 안 함" 안내**: `pantry_cleaned_at`이 비어있는 최근(3일 이내) 요리 기록이
    있으면(`fetchUncleanedRecentCookingLog`) 인사말과 분리된 액션 배너(`.action-banner`,
    신규 공통 CSS 클래스 — "인사말은 휘발돼도 되지만 이건 행동이 필요한 안내"라 명세가 명시적
    으로 분리를 요구함)로 "정리하기"를 노출, 탭하면 PantryTidyModal.
  - **B. 레시피**: 상시 노출되던 태그/알러지/보유재료 필터 칩 줄을 제거하고, 홈 검색 필드와
    같은 스타일의 pill 검색창(`.pill-input-row`, C-4와 공유) + 우측 필터 아이콘 버튼(적용된
    필터 개수 배지)으로 교체. 필터 아이콘 → 바텀시트(`RecipeFilterSheet`)에서 태그(다중)/
    알러지 제외/보유 재료로 가능/**조리시간(20·40·60분 이내, 신규)**/**난이도(신규)**/정렬을
    한 번에 고르고 로컬 draft 상태로만 바뀌다가 "초기화"/"적용"으로 확정. 적용된 필터만 검색창
    아래 칩으로 나열되고 칩의 X로 개별 해제. 목록 상단의 "🍳 여러개 요리하기" 버튼을 제거하고
    화면 우하단 고정 **FAB**으로 이동(`#root`의 `max-width:640px` 중앙 정렬을 감안해 calc로
    우측 위치 계산) — 단일 레시피 요리는 상세 화면 하단 버튼이 이미 주 경로라 FAB은 "여러 개
    요리하기" 전용 단일 버튼으로 충분하다고 판단, speed dial 없이 구현. **범위에서 뺀 것**:
    장보기 화면 필터(전체/구매 필요/보유)는 명세가 명시적으로 "대상이 다르니 적용하지 말 것"이라
    제외, 둘러보기(`DiscoverRecipesPage`) 화면의 자체 검색/필터는 데이터 구조가 달라(공개
    레시피, `PublicRecipeEntry`) 이번 개편 대상에서 제외(범위를 좁게 유지하기 위한 판단).
  - **실기기/수동 테스트 필요**: 이번 확장 전체가 자동 빌드 검증(`tsc -b`/`npm run build`/
    `npm run lint`)만 거쳤고 실제 화면 확인은 아직 안 됨 — SQL Editor에서 `0024_cooking_log_
    pantry_cleaned.sql`/`0025_shopping_extra_items.sql` 마이그레이션을 실행해야 D/E 기능이
    동작함. 특히 확인이 필요한 것: 냉장고 정리 모드의 탭 토글 애니메이션/카테고리 그룹핑,
    요리 완료 → 정리하기 흐름에서 실제로 "없어요" 박스가 미리 채워져 보이는지, 장보기 "직접
    추가" → "냉장고로 옮기기" 왕복, 홈의 "20분 안에 되는 것"/"이번 달 N번" 섹션이 실제 데이터
    로 자연스럽게 보이는지, 레시피 필터 바텀시트의 초기화/적용 동작, FAB이 다른 화면 요소와
    겹치지 않는지.
  - **실사용 피드백 반영(같은 세션)**: 필터 바텀시트의 "정렬"만 `<select>`로 남아있어 태그/
    조리시간/난이도(전부 칩 버튼)와 디자인이 어긋난다는 피드백 → 칩 버튼으로 통일. FAB을
    "여러개 요리하기" 전용에서 **"🍳 요리하기"**(하나 또는 여러 개)로 변경 — `MultiCookSelectPage`
    최소 선택 개수를 2→1로 낮추고 안내 문구도 수정, `RecipesFeature`가 선택 개수로 분기해서
    1개면 `RecipeDetailPage.autoStartCookingMode`로 바로 요리 모드 진입, 2개 이상이면 기존
    복합 요리 준비 화면으로.
- **25차 확장 완료 — 주간 일정 끼니 구분 + 한 끼 여러 메뉴**: 1(데이터 모델) → 2-3(UI) →
  5(연계 반영) 순으로 진행, 각 단계 커밋.
  - **데이터 모델**: `MealPlan`에 `mealType`('breakfast'|'lunch'|'dinner'|'snack', 기본
    'dinner')과 `sortOrder`(같은 끼니 안 순서) 추가(0026 마이그레이션). 기존
    `(household_id, date)` 유니크 제약은 "하루 한 행만" 가정이라 제거 — 같은
    `(household_id, date, meal_type)`에도 여러 행이 들어갈 수 있음(한 끼에 여러 메뉴).
    `src/data/mealPlans.ts`를 전면 재작성해 조회 결과가 `Map<date, MealPlan[]>`가 되고,
    `addMealPlan`/`updateMealPlanRecipe`("바꾸기")/`removeMealPlan`("빼기")로 개별 메뉴
    단위 CRUD를 제공(예전 `setMealPlan`/`clearMealPlan`은 날짜 단위 덮어쓰기라 폐기).
    `pickRepresentativeMealPlan`(저녁 우선, 없으면 가장 이른 끼니)을 홈/주간 일정이 공유.
  - **끼니 구분 UI — 설정 없이 접힘/펼침**: 저녁은 항상 펼침, 나머지(아침/점심/간식)는
    메뉴가 있으면 자동으로 펼쳐지고 없으면 "아침 ▸" 같은 조용한 한 줄(`.meal-section-collapsed`)
    로 접혀서 화면을 차지하지 않음 — 탭하면 펼쳐지고 그 자리에서 바로 "+ 메뉴 정하기"(점선
    박스)로 추가 가능. 펼침 상태는 저장하지 않고(메뉴가 있으면 자동으로 펼쳐지므로 저장할
    필요가 없음) 화면에 머무는 동안만 로컬 state로 기억.
  - **한 끼 여러 메뉴**: 저녁의 첫 번째 메뉴만 기존처럼 큰 카드(이미지+"레시피 보기"/"바꾸기"/
    "빼기"), 그 외 모든 메뉴(저녁의 두 번째부터, 다른 끼니는 전부)는 작은 행(`.meal-item-row`)
    으로 — 화면이 덜 복잡해지도록 판단. 메뉴가 있는 끼니엔 "+ 메뉴 추가" 버튼이 계속 있어
    원탭으로 추가/교체 가능(레시피 선택 모달이 바로 뜨고 고르면 즉시 배치 — 3단계로 늘어나지
    않음, 6번 원칙 유지). "바꾸기"(레시피 교체, `updateMealPlanRecipe`)와 "+ 메뉴
    정하기/추가"(신규 추가, `addMealPlan`)가 같은 `MealPlanRecipePicker` 모달을 공유하되
    `pickerContext.replaceId` 유무로 분기.
  - **요일 스트립(홈/주간 일정 공통)**: 그 날 대표 메뉴 하나(저녁 우선)만 표시하고 여러 개면
    "OO 외 N개"로 요약(전부 못 넣는 문제 해결) — `pickRepresentativeMealPlan` 재사용. 계획
    유무를 나타내는 점(`.week-day-dot.has-plan`)은 그대로 유지.
  - **연계 반영(5번)**: 홈 인사말의 "오늘은 OO예요"와 홈 주간 스트립이 같은 대표 메뉴 로직
    사용. `WeeklyPlanPage`의 "이어 쓰기" 재료 중복 감지 — 끼니 구분 없이 그 날 전체 메뉴
    (`dayRecipes`)의 재료 id를 모아 인접 요일과 비교하도록 변경(예전엔 그 날 유일한 레시피
    하나만 비교). "이 날 살 것"도 그 날 모든 메뉴의 재료를 합쳐(중복 제거) 안 보유 재료만
    나열. 영양 정보는 예전엔 "선택된 레시피 하나"의 칼로리만 보여줬는데, 이제 그 날 모든
    메뉴의 칼로리 합계(`recipe.nutrition.calories * recipe.servingsBase`를 레시피별로
    더함)를 페이지 상단에 한 줄로 표시.
  - **실기기/수동 테스트 필요**: 자동 빌드 검증만 거침 — SQL Editor에서
    `0026_meal_plans_meal_type.sql` 실행 필요. 특히 확인할 것: 끼니 섹션 접기/펼치기가
    메뉴 유무에 따라 기대대로 동작하는지, 저녁 두 번째 메뉴부터 작은 행으로 잘 보이는지,
    "바꾸기"가 그 메뉴 자리를 유지한 채 레시피만 바뀌는지, 여러 메뉴가 있는 날의 "이어
    쓰기"/"이 날 살 것"/영양 합계가 정확한지.
- **26차 확장 완료 — 실행 취소 + 인분 조절 전체 흐름 + 요리 모드 시안 반영 + 검색 확장 +
  주간 식단 자동 채우기**: 사용자가 첨부한 요리 모드 시안 2장을 참고해 **E(실행 취소) →
  B(인분 조절) → D(요리 모드) → C(검색) → A(주간 식단)** 순서로 진행(E가 공용 컴포넌트라
  먼저 만들면 나머지에서 재사용 가능, D-2가 B-5에 의존해서 B를 D보다 먼저). B는 장보기·주간
  식단·요리 모드·요리 기록 네 곳에 걸쳐 파급이 커서 **B-3(장보기)까지 끝난 시점에 사용자
  실사용 테스트를 거친 뒤** 나머지를 이어감. 구간마다 커밋.
  - **E. 실행 취소(Undo)**: `src/data/undoToast.ts`(신규, `useSyncExternalStore` 패턴) —
    `showUndoToast(message, onUndo)`로 5초짜리 토스트(`.toast-undo`)를 띄우고, 그 안에서
    "실행 취소"를 누르면 등록해둔 복구 콜백만 실행(변경 전 스냅샷을 호출부가 직접 들고 있다가
    `saveX(snapshot)`으로 되돌리는 방식 — 별도 되돌리기 전용 API 없이 기존 save/delete
    함수 재사용). 되돌리기 어려운 작업 5곳에 연결: 냉장고 정리 모드 적용(`PantryTidyModal`),
    재료 삭제, 레시피 삭제, "오늘 만들었어요"의 재료 차감(`RecipeDetailPage`), 장보기→냉장고
    이동(`MoveToFridgeModal`). 토스트가 떠 있는 5초 동안만 취소 가능, 지나면 그냥 사라짐.
  - **B. 인분 조절 — 전체 흐름 반영**: 실사용 중 발견된 버그 — 레시피 상세의 인분 조절이
    로컬 state뿐이라 DB에 저장되지 않고, "장보기에 담기"를 눌러도 그 인분이 장보기로 안
    넘어가서(`shopping_selection`이 `recipe_id`만 저장) 장보기 집계가 항상
    `recipe.servingsBase` 기준으로만 계산됐음("4인분으로 담았는데 실제로는 2인분" 버그).
    **가구 기본 인원(전역 기본값) + 항목별 인분(개별 저장)** 조합으로 설계.
    - `households.default_servings`(0027, 기본 2) — 프로필 바텀시트의 가구 설정에서 변경
      가능. `recipe.servingsBase`(레시피 원본 기준)와는 별개 개념(가구 습관 vs 레시피 원본).
    - `shopping_selection.servings`(0028, B-3, **사용자 테스트 완료**) — "장보기에 담기" 시
      현재 화면 인분을 저장, 장보기 집계를 `scaleAmount(amount, servingsBase, servings)`로
      재계산, 레시피별 인분을 장보기 화면에서 개별 수정 가능(수정 시 즉시 재집계). 표시는
      "베이컨 120g · 된장 크림 파스타 4인분" 형식. 기존 데이터는 `servingsBase`로 백필.
    - `meal_plans.servings`(0029, B-4) — 메뉴 배치 시 가구 기본 인원이 초기값, 카드에서
      스테퍼로 개별 조절 가능. "이 날 살 것"/영양 합계도 이제 `recipe.servingsBase`가 아니라
      실제 배치된 `plan.servings` 기준.
    - **B-5(요리 모드 진입 시 인분)** — 중앙 resolver 함수 없이 진입점마다 `initialServings`
      prop을 직접 계산해서 넘기는 방식으로 처리: (1) 주간 일정 "바로 요리하기"→그 `MealPlan.
      servings`, (2) 레시피 상세에서 "요리 시작하기"→화면에서 조절 중이던 `servings` state,
      (3) 홈 "오늘의 추천"/레시피 목록 FAB처럼 상세 화면을 거치지 않는 지름길→가구 기본 인원
      (사용자가 실제로 인분을 본 적이 없는 진입이라 레시피 원본 대신 가구 습관을 기본값으로
      판단).
    - `cooking_log.servings`(0030, B-6) — 요리 후 재료 차감도 이 인분 기준으로 스케일링.
      **범위 제한**: 복합 요리(여러 레시피 동시 진행)는 레시피별 인분 조절 UI가 없어
      `servings: recipe.servingsBase`로 중립 기본값 사용(코드 주석으로 남김).
    - B-7(UI 어디서나 인분 기준 명시)은 위 B-2~B-6 각 화면에 이미 자연히 반영되어 별도 작업
      없이 충족됨.
  - **D. 요리 모드 — 시안 반영**: 상단을 X + 진행 바(`.cooking-mode-progress-bar`, 7px,
    accent 색, width 전환 .35s) + "n/총" + 레시피 이름(작게)으로 재구성, 단계 문구를 34px로
    확대, 타이머를 카드(`.cooking-mode-timer-card`)로 분리. **"이 단계에서 쓰는 재료" 칩
    (신규, `src/lib/stepIngredientMatch.ts`의 `findStepIngredients`)** — 재료 사용 여부를
    나타내는 전용 매핑 필드가 데이터 모델에 없어서, 단계 제목+본문 텍스트에 재료 이름이
    포함되는지로 매칭하는 의도적으로 단순한 휴리스틱(수량은 B-5로 확정된 인분 기준
    `scaleAmount` 반영). **팁 박스** — `RecipeStep.tip?: string`을 추가했는데, `RecipeStep`은
    `recipes.content`(jsonb) 안에 저장되는 필드라 **DB 마이그레이션이 필요 없음**(TypeScript
    타입 추가만으로 충분 — jsonb는 스키마리스). 편집 화면에 단계별 팁 입력 필드 추가,
    `propose_recipe` 스키마(Claude/Gemini 둘 다)와 시스템 프롬프트에도 `tip` 필드 반영(모든
    단계를 억지로 채우지 않도록 안내). 완료 화면은 큰 원형 일러스트 + "잘 만들었어요!" +
    "오늘 요리 n번째예요"(`fetchTodayCookingCount` 신규) — 명세의 "유통기한 임박 재료
    N% 소진" 통계는 이 앱의 재료가 boolean(`owned`)이라 소진율 계산이 불가능해서(명세에
    "애매하면 요리 횟수만 표시" 허용 문구가 있어) 요리 횟수만 표시. 음성 명령/명령어 목록은
    기존 그대로, 새 레이아웃에 위치만 재배치.
  - **C. 검색 확장**: (C-1) `src/lib/recipeSearch.ts`(신규) — 검색 대상에 태그 이름(스타일/
    카테고리/국가 전체 축) + 조리 단계 제목·본문을 추가. "쉬운"/"쉬움"/"초보"→난이도 easy,
    "어려운"/"어려움"→hard, "빠른"/"간단한"/"간단"/"빨리"→예상 조리시간 20분 이하로 키워드를
    먼저 추출·제거한 뒤 나머지 텍스트로 일반 매칭(예: "쉬운 파스타" = 난이도 필터 AND 텍스트
    "파스타" 매칭). (C-2) 직접 입력해서 만든 레시피는 AI 대화/유튜브 변환을 안 거쳐 태그가
    안 붙는 문제 보완 — 저장 시 태그가 하나도 없으면 이름+재료+조리순서를 근거로
    `suggestRecipeTags`(claudeClient.ts/geminiClient.ts, `api/ai-tags.ts` — ai-nutrition.ts와
    같은 Vault 경유 두 제공자 분기 패턴)가 태그 2~4개를 제안하고, "이런 태그 어때요?" 모달
    (`TagSuggestionModal`)에서 사용자가 선택한 것만 실제로 붙는다(자동 적용 금지). 제안
    실패(API 키 없음 등)는 저장을 막지 않고 조용히 건너뜀.
  - **A. 주간 식단 — 메뉴 정하기 개선**: (A-1) 메뉴 선택 모달의 검색 input을 홈 검색 필드와
    같은 톤(`.pill-input-row` + 좌측 search 아이콘)으로, 목록 항목은 새로 만들지 않고 기존
    `RecipeListItem`(썸네일+메타)을 재사용해 정리. (A-2) 모달 상단에 "🔁 자주 해먹는
    메뉴"(`fetchCookingStats` 횟수 상위 4개)/"🧺 지금 재료로 가능"(`isRecipeMakeableWithPantry`
    전부 통과 상위 4개) 가로 스크롤 추천 섹션 — 기존 `RecipeCard(size="row")` 재사용, 탭하면
    검색 결과와 동일하게 원탭 배치, 검색어 입력 중이거나 데이터가 없으면 섹션이 숨음.
    (A-3) **"✨ 이번 주 자동으로 채우기"**(`src/lib/mealPlanAutoFill.ts`, 순수 함수·규칙
    기반) — 저녁 메뉴가 없는 날만 대상으로, 보유 재료 매칭률(가중치 3) + 자주 해먹은
    정도(가중치 2, 최대 5회 캡) + 오래 안 해먹은 정도(가중치 1, 최대 30일 캡, 한 번도
    안 해먹었으면 최댓값)를 합산한 점수로 정렬하되, 유통기한 임박(경과/3일 이내 — 홈
    인사말과 같은 "soon 제외" 기준) 재료를 쓰는 레시피는 점수와 무관하게 항상 앞쪽으로
    정렬해 이른 요일부터 배정되게 한다. 같은 레시피가 연달아 배정되지 않도록 커서를
    순환시키며 다음 후보를 찾는다. **자동 반영 금지** — 계산 결과는 미리보기 모달
    (`AutoFillPreviewModal`)로만 보여주고 "이대로 채우기"를 눌러야 실제 저장됨, "다시
    짜기"는 시작 커서를 랜덤하게 바꿔 다른 조합을 재계산(순수 규칙 기반이라 같은 입력에
    같은 커서면 항상 같은 결과이므로). 이미 메뉴가 있는 날은 대상에서 애초에 제외되어
    안 건드림, 배치 후 마음에 안 드는 날은 기존 "바꾸기" 버튼으로 개별 변경.
  - **실기기/수동 테스트 필요**: B-3(장보기 인분 반영)만 사용자가 직접 테스트 완료, 나머지는
    자동 빌드 검증(`tsc -b`/`npm run build`/`npx oxlint`)만 거침 — SQL Editor에서
    `0027_household_default_servings.sql`~`0030_cooking_log_servings.sql`(4개) 실행 필요.
    특히 확인할 것: 주간 식단/요리 모드 진입 지점별 인분이 기대한 값으로 시작하는지(B-5 4가지
    경로), 요리 모드 새 레이아웃과 "이 단계에서 쓰는 재료" 칩 매칭 정확도, 팁 입력/표시,
    완성 화면의 "오늘 요리 n번째", 검색창에 "쉬운"/"빠른" 같은 키워드를 섞었을 때 결과,
    수동으로 레시피를 만들 때 태그 제안 모달이 실제로 뜨는지와 제안 품질, 주간 식단 모달의
    추천 섹션 두 종류, "이번 주 자동으로 채우기"가 실제로 그럴듯한 조합을 내는지와 "다시
    짜기"가 다른 조합을 주는지.
- **27차 확장 완료 — 유통기한 처리 정비(상태 판정 + 시각 위계)**: 상태 판정 로직과 시각 표시가
  얽혀있는 문제를 함께 해결. **A-3(공용 판정 함수) → B(시각 표시) → A-4~A-7(각 기능 반영)**
  순서로 진행, 구간마다 커밋.
  - **핵심 원칙 — 앱은 식품 안전을 판단하지 않음**: 유통기한이 지났다고 자동으로 owned:false
    처리하지 않고(재료마다 실제 먹을 수 있는 기간이 다르고 보관 상태에 따라서도 달라짐),
    그렇다고 owned:true라는 이유로 "있음"으로 취급하지도 않는다(있다고 믿고 장을 안 봤는데
    막상 상해있는 상황 방지) — 사용자가 직접 확인하기 전까지는 중간 상태로 남겨둔다.
  - **A-3. 공용 판정 함수**(`src/lib/pantryAvailability.ts`, 신규): `getPantryAvailability`가
    재료 하나를 usable(보유+안 지남)/expired_unconfirmed(보유+지남+확인 전)/unavailable(없음)
    3단계로 판정. `isRecipeMakeableWithPantry`(computed.ts)를 시작으로 "보유 재료로 가능"
    판단이 필요한 모든 곳(레시피 추천, 둘러보기 재료 매칭, 주간 식단 자동 채우기 점수, 장보기
    "구매 필요" 집계, 재고 선제 제안)이 이 함수를 공유하도록 순차 반영.
  - **B. 시각 위계 재설계**: "지남"이 "D-2 이내"보다 약하게(회색 아웃라인 vs 빨간 배경) 표시되던
    역전 버그 수정. 3단계로 재구성(급한 순): 지남(danger 채워진 배경+대비 텍스트+굵게, 문구에
    ⚠️ 포함) > D-2 이내(앰버 연한 배경+진한 텍스트) > D-3~7(연한 테두리만, 아이콘 없음). urgent
    day 경계를 3일→2일로 조정(D-0은 "오늘까지"로 표기). `--warning` 토큰 값 자체도 교체 —
    기존엔 accent-700(테라코타 계열)을 재사용해서 accent와 색상 계열이 겹쳐 육안 구분이 안
    됐던 문제라, 진짜 앰버/골드 색상으로 새로 잡음(WCAG AA 대비 검증: bg-card 위 텍스트 기준
    라이트 5.71:1/다크 5.29:1). 정렬(지난 것이 위)은 기존 daysLeft 오름차순 정렬이 지남(음수)을
    자연히 앞에 둬서 별도 변경 불필요.
  - **A-4. 사용자 확인 흐름**(신규): 냉장고 목록에서 유통기한 지난 재료를 탭하면 상세 설정
    모달 대신 `ExpiredConfirmModal`(IngredientsPage.tsx에서 export, 장보기/주간 식단 화면도
    재사용)이 먼저 뜬다 — "아직 괜찮아요"(유통기한을 비워 usable로 복귀, "자세히 수정하기"로
    새 날짜 입력도 가능)/"버렸어요"(owned:false). 정리 모드(PantryTidyModal)에 "⏰ 유통기한
    지난 재료 확인하기 (N개)" 액션 추가 — `ExpiredReviewModal`이 지난 재료를 목록으로 보여주고
    각각 즉시 처리(있어요/없어요 박스의 "적용 눌러야 저장" 드래프트 방식과 달리, 개별 확정
    액션이라 바로 저장).
  - **A-5. 각 기능 반영**: "이 날 살 것"(WeeklyPlanPage)/장보기 집계(ShoppingListPage)는
    usable이 아닌 재료를 전부 포함하되 확인 필요 재료는 "유통기한 지남 — 확인 필요" 별도
    표시(체크/토글도 danger 톤으로 구분, 탭하면 확인 모달). "보유 재료로 가능" 필터, 홈
    추천(recipeRecommendation.ts), 둘러보기 재료 매칭(DiscoverRecipesPage.tsx)은 전부 usable
    기준. 홈 추천 카드의 "재료 N개 중 M개 있어요" 배지에 확인 필요 재료가 있으면 "확인 필요
    K개" 별도 표기. 주간 식단 자동 채우기(mealPlanAutoFill.ts)의 "임박 재료 레시피 앞당기기"는
    이미 지난(expired) 재료는 제외하고 아직 안 지난 임박(urgent)에만 반응 — 앱이 "지난 재료로
    요리해도 된다"고 판단하는 것처럼 보이면 안 되므로.
  - **A-6. "이어 쓰기" 모순 수정**: 겹치는 재료가 유통기한이 지나 확인이 필요한 상태면 "이어
    쓰기" 대상에서 제외하고 "{재료}은(는) 유통기한이 지났어요. 상태를 확인해주세요."로 안내
    (이미 상한 걸 이어서 쓸 수는 없으므로). 아직 안 지났지만 임박한 재료는 기존대로 "이어 쓰기"
    대상 유지("빨리 써야 하니 이어서 쓰자"는 취지에 부합). 유통기한 확인 자체도 이제 owned인
    재료만 대상으로 좁힘(소유하지 않은 재료엔 실제 유통기한 개념이 없음 — 이전엔 owned 무관하게
    확인해서, 예전에 소유했다가 지금은 아닌 재료의 남은 유통기한 값이 잘못 노출될 수 있었음).
  - **A-7. 홈 인사말 최우선 순위**: "유통기한 지남"을 다른 모든 안내(오늘 요리함/오늘 식단/
    임박/장보기)보다 먼저 보여준다 — "{재료}, 유통기한이 지났어요. 확인해보세요"(homeGreeting.ts).
    확인이 필요한 상태라는 사실이 가장 먼저 전달돼야 한다고 판단.
  - **부수 버그 수정**: 냉장고/홈의 "유통기한 임박/경과" 요약이 owned 여부와 무관하게 전체
    재료를 훑던 문제(보유하지 않은 재료도 유통기한이 설정돼 있으면 표시됨) — owned인 것만
    대상으로 좁힘. 유통기한 임박 알림(Vercel Cron, `check-expiring-ingredients.ts`)도 같은
    이유로 `eq('owned', true)` 필터 추가.
  - **실기기/수동 테스트 필요**: 자동 빌드 검증(`tsc -b`/`npm run build`/`npx oxlint`)만
    거침, DB 마이그레이션 없음(기존 `owned`/`expirationDate` 컬럼만 사용). 특히 확인할 것:
    지난 재료 탭 시 확인 모달이 뜨는지와 "아직 괜찮아요"/"버렸어요" 처리, 정리 모드의 "유통기한
    지난 재료 확인하기" 목록, 배지 3단계 색이 라이트/다크 모두 잘 구분되는지, 장보기/주간 식단
    화면의 "확인 필요" 표시와 확인 모달 연결, "이어 쓰기" 문구가 지난 재료일 때 올바르게
    전환되는지, 홈 인사말이 지난 재료가 있을 때 최우선으로 뜨는지, 웹 푸시 알림이 실제로
    owned=false 재료를 건너뛰는지(Cron이라 즉시 확인은 수동 호출 필요).
- **28차 확장 완료 — "있는 재료로 만들기" + 레시피 편집 화면 정리 + 장보기 카테고리 그룹핑**:
  타 앱 리서치(Mealime/Paprika/AnyList 등 2026년 밈플래닝 앱, ChefGPT/DishGen 등 AI 레시피 앱)
  결과 "보유 재료 기반 AI 레시피 생성"이 공통 핵심 기능이라는 걸 확인하고 우선 구현.
  - **홈 "🍳 있는 재료로 만들기"**: 지금 usable한(유통기한 지나 확인 필요한 건 제외) 재료
    이름을 모아 대화형 레시피 생성을 원탭으로 시작. `RecipeChatPanel`에 `autoSendText` prop을
    추가해 채팅을 열자마자 첫 메시지를 자동 전송(ref로 StrictMode 이중 실행 방지),
    `RecipeEditor`/`HomePage`의 `View` 타입도 `recipeId` 없이(신규 레시피) 열 수 있게 확장.
    `RECIPE_CHAT_SYSTEM_PROMPT`에도 "보유 재료 목록을 주면 최대한 활용하고, 꼭 필요한데
    없는 주재료만 짧게 언급" 규칙 추가(이 기능의 전제 조건).
  - **레시피 편집 화면(RecipeEditor.tsx) UI 정리**: 실사용자 피드백 반영 — "대화로 레시피
    만들기/수정하기" 안내 문구를 5줄→1줄로 축소(웹 검색 체크박스는 이미 자기 라벨로 설명되니
    중복 제거), `RecipeChatPanel` 자체 h2 제목과 중복되던 상위 section-title 제거. 유튜브
    변환 섹션도 자막 추출 폴백 체인(한국어→영어→자동생성→AI 음성인식) 같은 구현 세부사항
    설명을 없애고, "유튜브에서 변환"→"유튜브 변환"으로 축약 + 대화형 패널의 "보내기" 버튼처럼
    입력창 오른쪽에 배치(일관성). "기본 정보"부터 시작하는 실제 저장 영역이 위쪽 AI 도구
    영역과 잘 구분되도록 굵은 구분선+제목("✏️ 레시피 내용", `.recipe-editor-divider`) 추가.
  - **장보기 카테고리(매대) 그룹핑**: `ShoppingListPage`의 재료 목록을 평평한 리스트 대신
    재료 카테고리별 섹션으로 묶음 — 매장 동선대로 한 구역씩 확인하며 장 보기 편하게. 카테고리
    순서는 카테고리 관리의 등록 순서를 그대로 따르고, 삭제된 재료는 "기타"로 묶는다. 행
    렌더링을 `ShoppingItemRow` 컴포넌트로 분리(그룹마다 반복 렌더링 필요, 로직 변경 없는
    순수 리팩터).
  - **범위에서 미룬 것**: 리서치에서 함께 발견한 바코드 스캔(제품 DB 연동이 새로 필요해서
    보류)과 냉장고 사진 일괄 인식(영수증과 달리 물건 경계가 지저분해 인식 정확도 리스크가
    커서 보류)은 이번엔 진행하지 않고 아이디어로만 남겨둠. 대화형 프롬프트의 "가구 알러지
    자동 인지"(재료에 이미 등록된 allergens를 매 대화마다 자동으로 피하도록 안내) 방향도
    논의는 됐으나 이번 배치에서 구현하지 않음 — 다음에 진행할 후보.
  - **실기기/수동 테스트 필요**: 자동 빌드 검증만 거침. 특히 확인할 것: 홈 "있는 재료로
    만들기"가 실제로 재료를 반영한 레시피를 제안하는지, 자동 전송된 첫 메시지가 채팅에
    올바르게 나타나는지, 유튜브 변환 버튼 위치/문구, "기본 정보" 구분선이 실제로 눈에 띄는지,
    장보기 카테고리 그룹핑이 실제 재료 구성으로 자연스럽게 보이는지.
- **29차 확장 완료 — 가구 알러지 관리(프로필) + 대화형 AI 알러지 확인 + 기본 태그 확장**:
  실사용 중 "재료마다 알러지를 하나씩 등록하는 게 번거롭다"는 피드백 → 하이브리드로 해결.
  - **household.allergens(신규, 0032)**: 프로필 > 가구 설정에 "알러지 관리" 섹션 추가 —
    가구 단위로 "마늘, 밀가루" 같은 목록을 한 번에 관리. 추가하면 이름이 정확히 같은 기존
    재료(예: "마늘")는 자동 태깅. "돈까스소스"처럼 이름만 봐선 알 수 없는 재료는 그 재료
    상세 화면(IngredientDetailModal)에서 표시해야 하는데, 이제 그 화면의 "알러지 유발 성분"
    섹션이 자유 텍스트 대신 가구 목록을 토글 칩으로 제안해서 탭 한 번으로 끝남(목록에 없는
    것만 직접 입력). 레시피 알러지 필터(computeRecipeAllergens)는 그대로 ingredients.
    allergens 집계 방식이라 변경 없음 — 이 목록은 입력을 쉽게 하는 관리용.
  - **대화형 AI 알러지 확인**(아이디어 논의는 이번 회차, `ExistingContext.allergenIngredients`
    는 앞 커밋에서 이미 구현): household.allergens가 아니라 여전히 ingredients.allergens
    기반이라 위 UI 개선과 자연히 호환됨 — 알러지 등록된 재료가 필요한 레시피를 제안하기 전에
    AI가 먼저 확인 질문을 한다.
  - **기본 태그 확장(0031)**: 국가/장르(cuisine) 5개→9개(동남아식/인도식/멕시칸/분식 추가),
    요리 스타일(style)은 0개→12개 신규 시딩(크림류/토마토류/고기요리/국물요리/볶음요리/
    구이요리/튀김요리/찜·조림/면요리/밥·죽류/무침·샐러드/매콤한맛) — "귀찮은 사용자"도 태그
    관리에서 직접 만들 필요 없이 바로 고를 수 있게. 기존 household는 이름별로 없는 것만
    채워넣고(중복 방지), 새 household는 create_household RPC가 자동으로 받음.
  - **schema.sql 동기화(발견 및 수정)**: create_household/join_household_by_invite_code RPC가
    지금까지 마이그레이션(0002, 0005)에만 있고 schema.sql엔 아예 없던 걸 발견 — 신규 설치 시
    이 RPC 자체가 누락되는 문제였음. 이번에 schema.sql에 채워넣어 동기화.
  - **실기기/수동 테스트 필요**: SQL Editor에서 `0031_expand_default_tags.sql`,
    `0032_household_allergens.sql` 실행 필요(0032 실행 전엔 households.allergens 컬럼이
    없어서 관련 화면이 에러 남). 확인할 것: 프로필에서 알러지 추가 시 이름이 같은 재료가
    실제로 자동 태깅되는지, 재료 상세 화면의 토글 칩이 가구 목록을 잘 반영하는지, 대화형
    AI가 알러지 재료가 필요한 요청에 실제로 확인 질문을 하는지, 기존 household에 새 기본
    태그가 잘 채워지는지.
- **30차 확장 완료 — 요리 기록 사진 갤러리 + 직접 추가(과거 날짜) + 유튜브 변환 버튼 버그 +
  주간 일정 명칭 개선**: 1(유튜브 변환 버그+카드 제목) → 2(요리 기록 직접 추가) → 3(이어 쓰기
  명칭) → 4(사진 갤러리, 4-1 먼저·4-2/4-3 이어서) → 5(대표 사진 설정 보완) 순서로 진행, 구간마다
  커밋(1: `97aa443`, 3: `d3df7d3`, 2: `14d95f8`, 4: `cae0401` — 2·3은 서로 다른 파일이라 3을
  먼저 끝냈고, 5는 2번 구현 중 사진 업로드 흐름을 만들면서 자연스럽게 같이 반영되어 별도
  커밋 없이 2번 커밋에 포함됨).
  - **1. 유튜브 변환 버튼 세로 눌림 버그**(실기기 캡처로 발견): `.row`(flex) 안에서 `flex:1`
    형제(유튜브 링크 입력창)가 공간을 차지할 때 버튼이 내용보다 좁게 눌리고, 한글은 글자
    단위로도 줄바꿈되는 기본 규칙 때문에 "유튜브 변환" 텍스트가 "유/튜/브/변/환"처럼 세로로
    쌓이던 문제 — 원인은 버튼에 `white-space: nowrap`이 없어서(input은 브라우저가 강제하는
    최소 너비가 있어 안 눌리지만 버튼 텍스트는 그런 하한이 없음). 개별 버튼이 아니라 전역
    `.btn`에 `white-space: nowrap`을 추가해 같은 패턴(`.row` 안에 flex:1 형제 + 버튼)을 쓰는
    다른 화면(대화형 채팅 패널의 "보내기" 버튼 등)도 함께 근본 수정. 360~430px 5개 뷰포트를
    Playwright로 직접 스크린샷 찍어 검증(재사용 가능한 뷰포트 테스트 스크립트는 세션
    스크래치패드에만 있고 레포에는 커밋하지 않음). "또는 유튜브 링크로 변환" 본문 텍스트도
    대화형 섹션("💬 대화로 레시피 만들기/수정하기")과 대칭인 카드 제목("🎬 유튜브 링크로
    변환")으로 변경.
  - **2. 요리 기록 직접 추가**(`CookingHistoryPage`의 "+ 기록 추가" → `AddCookingLogModal`
    신규): 레시피 상세를 거치지 않고 과거 날짜의 요리도 기록할 수 있는 보완 경로("오늘
    만들었어요" 흐름은 그대로 유지). 레시피 선택(검색 가능) → 날짜 선택(기본 오늘, 과거
    가능·미래 금지) → 완성 사진 업로드(선택) → 재료 정리(선택) 4단계. 인분 조절 UI는 없어
    복합 요리와 같은 이유로 `recipe.servingsBase` 고정. **재료 체크리스트 기본값을 "오늘
    만들었어요"(기본 전체 선택)와 반대로 전부 미선택으로 시작** — 과거 기록은 지금 냉장고
    상태와 무관할 수 있어서, 사용자가 실제로 그때 다 썼다고 기억하는 것만 직접 체크하게
    설계(강제 안 함). `logCooking()`에 `cookedAt?: string` 옵션 추가(생략 시 DB 기본값
    `now()` 유지, 날짜만 고르므로 자정 경계 타임존 문제를 피하려 항상 정오로 고정해서 넘김).
  - **`cooking_log.image_id`(신규, 0033)**: 요리 기록에 사진을 붙일 수 있는 컬럼 — 레시피의
    대표 사진(`recipes.final_image_id`)과는 별개로 "그 한 번의 요리"를 남기는 사진. 대표 사진
    지정 여부와 무관하게 항상 이 컬럼에 연결해서(`setCookingLogImage`), 대표로 안 해도 사진이
    Storage에만 남고 아무 데서도 참조되지 않아 유실되는 일이 없게 함 — 4번 갤러리의 근거
    데이터. `ImageKind`에 `'log'` variant 추가.
  - **5. 대표 사진 설정 보완**(원래 스펙은 2·4번 이후 마지막 단계였지만 2번 구현 중 자연히
    같이 반영됨): "오늘 만들었어요"(`CookingLogModal`)/"+ 기록 추가"(`AddCookingLogModal`)
    두 진입점 모두, 사진을 올렸는데 레시피에 이미 대표 사진이 있으면 "대표 사진으로
    설정할까요?" 확인 옆에 **지금 대표 사진 48px 썸네일을 나란히 보여줘서 비교**할 수 있게
    하고, "아니어도 '우리집에서 만든 모습' 갤러리에는 남아요" 안내를 추가해 대표 사진으로
    지정하지 않아도 사진 자체가 사라지는 게 아니라는 걸 알려줌(선택 부담 완화).
  - **4. 요리 기록 사진 갤러리** — `CookingLogPhotoGallery`(신규, `collapsible` prop으로
    두 화면 겸용) 공유 컴포넌트:
    - **4-1(RecipeDetailPage)**: 완성 사진 아래 "📸 우리집에서 만든 모습 (n)" 가로 스크롤
      갤러리, 항상 펼쳐진 상태(`collapsible` 미지정). 그 레시피의 cooking_log 중 사진 있는
      기록을 최신순으로, 각 항목에 날짜+만든 사람 이름(`profiles.display_name`) 표시, 탭하면
      라이트박스.
    - **4-2(PublicRecipeDetailPage, 둘러보기)**: 다른 household의 요리 기록을 다짜고짜
      노출하지 않기 위해 다르게 설계 — "🍽 이 레시피를 요리해본 사람들 (n)"을 접힌 상태로
      시작(`collapsible` prop, 탭해야 펼침), 개인 이름 대신 **household 이름만**
      표시("영희네에서 만든 모습", 프라이버시 보호). 사진이 하나도 없으면
      `CookingLogPhotoGallery`가 `items.length===0`일 때 아무것도 렌더링하지 않는 걸 그대로
      활용해 접기 버튼 자체가 안 보임(별도 분기 불필요).
    - **4-3(RLS, 0034)**: `cooking_log_select_via_public_recipe`(공개 레시피가 참조하는
      cooking_log 행은 다른 household도 SELECT 가능)와
      `households_select_via_public_cooking_log`(그 행이 가리키는 household의 이름도 읽을 수
      있어야 갤러리에 이름을 표시할 수 있음 — 기존 `households_select_via_public_recipe`(0012)는
      "레시피 작성자의 household"만 커버해서 "그 레시피를 요리해본 다른 household"까지는
      포함하지 않아 별도 정책이 필요했음) 두 정책을 tags/ingredients/profiles에 이미 있던
      "공개 레시피가 참조하는 경우에 한해 SELECT 허용" 패턴 그대로 추가. `memo`/재료 사용
      세부정보는 select 자체에서 빼서 애초에 가져오지 않음(4-2 조회 함수
      `fetchPublicCookingLogPhotos`가 `id, image_id, cooked_at, households(name)`만 select).
  - **3. 주간 일정 "이어 쓰기" 명칭 개선**: 끼니 이름("간식" 등)과 나란히 있으면 헷갈릴 수
    있다는 지적 반영 — "이어 쓰기" → "🔗 재료 이어쓰기"로 아이콘+명확한 이름 변경. 박스
    자체는 이미 색상 배경(success 톤)으로 끼니 섹션 제목과 시각적으로 구분돼 있어 스타일
    변경은 하지 않음.
  - **실기기/수동 테스트 필요**: 전부 자동 빌드 검증(`tsc -b`/`npm run build`/`npx
    oxlint`)만 거침 — 1번의 뷰포트 확인만 Playwright로 실제 스크린샷을 찍어 검증했고
    나머지는 화면 확인이 안 됨. SQL Editor에서 `0033_cooking_log_image.sql`,
    `0034_cooking_log_public_recipe_rls.sql` 실행 필요(0033 실행 전엔 사진 업로드/갤러리
    관련 화면이 에러 남). 특히 확인할 것: "+ 기록 추가" 4단계 흐름이 실제로 자연스러운지(특히
    재료 체크리스트가 기본 미선택으로 시작하는 게 맞게 느껴지는지), 두 진입점의 대표 사진
    비교 썸네일이 실제로 보기 좋은지, 레시피 상세 갤러리와 둘러보기 갤러리(접기/펼치기,
    household 이름 표시)가 실제 데이터로 잘 보이는지, 라이트박스 동작, 유튜브 변환 버튼이
    실기기에서 정상적으로 가로로 보이는지.

## 기술 스택 / 아키텍처 결정
주제별 상세 설계 결정, 버그 원인, 트레이드오프는 `docs/architecture.md`로 옮겨졌습니다(프론트엔드
구조/탭 전환, Supabase 전환, 이미지 저장, API 키 Vault, 요리 모드, 복합 요리 등 24개 주제 —
목차는 그 파일 상단 참고). 관련 기능을 다루게 되면 `docs/architecture.md`를 직접 Read할 것 —
CLAUDE.md와 달리 자동으로 로드되지 않는다.

## 향후 확장 계획 (지금부터 구조는 열어두되 구현은 나중에)
- **로그인 화면 UX 개선**: 이메일 로그인 흐름을 화면 전환(예: 확인 이메일 발송 화면)까지는 개선했지만,
  전체적으로 더 사용자 친화적으로 다듬을 여지가 있음(구체적인 방향은 아직 미정 — 나중에 다시 검토)
- **다중 사용자**: 부부가 같이 보고 수정할 수 있게 — 위 "DB 전환(Supabase)" 항목에서 진행 중
- **손님초대모드(메뉴 세트)**: 여러 레시피를 묶어서 하나의 "메뉴 세트"로 저장/재사용 (아래 데이터 모델에 스텁 포함)
- **가구 간 팔로우/구독(레시피 visibility 다음 단계)**: 지금은 다른 가구 레시피를 보려면 "전체 공개"뿐이라
  전부 아니면 전무 식인데, 사용자가 많아지면 "이 가구만 팔로우해서 그 가구가 공개한 레시피는 항상 보기"
  같은 가구-대-가구 연결 관계를 만들고 싶다는 아이디어(2026-07-29 논의). 지금 3단계
  (private/household/public) 구조 위에 `household_follows`(follower_household_id, followed_household_id)
  같은 테이블을 얹으면 될 것 같음 — 예를 들어 "팔로우한 가구의 household 등급 레시피까지 보이게" 정책을
  추가하는 식. 사용자 수가 늘어나서 "전체 공개 둘러보기"만으로는 관계性이 부족해질 때 진행하기로 하고
  지금은 스킵.

## 핵심 기능 요구사항

### 0. 로그인 화면
- "CookKit" 타이틀 + **구글 로그인**(메인) 버튼. 아래에 "이메일로 로그인" 링크를 누르면 이메일/비밀번호
  로그인·회원가입 폼이 펼쳐짐(구글 계정이 없거나 네트워크 제약으로 구글 접속이 막힌 환경 대응 — 정식 기능)
- 로그인 후 소속 household가 없는 신규 유저는 "가구 만들기/초대코드로 참여하기" 온보딩 화면으로 유도됨
  (`HouseholdOnboarding.tsx`)

### 1. 재료(보유현황) 관리 화면
- 카테고리는 고정 2종이 아니라 **사용자가 추가/이름변경/삭제할 수 있는 항목**(카테고리 관리 화면 별도 존재).
  기본 시드: 채소, 육류·해산물, 유제품, 곡류, 소스·양념, 기타
- UI는 카테고리별 접기/펼치기가 가능한 컴팩트 리스트(한 줄에 이름+보유토글, 알러지는 아이콘/개수로 축약)
- 각 재료의 보유 여부(있음/없음)를 토글로 관리, 레시피와 독립적으로 저장
- 각 재료에 **알러지 유발 성분 태그**(allergens)를 등록/수정하는 액션 포함
  (예: 마늘 → allergens: ['마늘'], 토마토파스타소스 → 제품에 따라 ['마늘'] 붙일 수도 있음)
- **재료별 개인 선호 설정**: 알러지와 같은 "상세 설정" 모달(`IngredientDetailModal`)에서 선호 계량 단위
  (`preferredUnit`, COMMON_UNITS 중 선택)와 선호 방식(`preferredMethod`)을 함께 설정. 방식은 자유 텍스트를
  매번 입력하게 하면 불편해서 **자주 쓰는 프리셋 칩**(다진 것/편으로/그라인더로/가루로/생것 그대로) 중
  선택하는 방식이 기본이고, "직접입력" 칩을 고를 때만 자유 텍스트 입력창이 나타남(진짜 특이한 경우만
  텍스트로). 온보딩으로 몰아서 받지 않고 그 재료 편집 화면에서 그때그때 설정하는 방식. 설정된 재료는 AI
  레시피 생성/수정 프롬프트에 참고 정보로 자동 전달되어(`buildExistingContextNote`) 레시피에 반영됨
  (예: 소금이 "선호 단위: 작은술"이면 그 단위로 표기 시도)

### 2. 레시피 관리 화면 (앱의 메인 화면)
- 기본 진입 탭 — 레시피를 보다가 장보기로 자연스럽게 이어지는 흐름의 중심
- 레시피를 쉽게 추가할 수 있어야 함:
  - **대화형 AI로 레시피 만들기/수정하기** (`RecipeChatPanel.tsx`) — 텍스트 설명을 처음 입력하는 것과
    이미 있는 초안을 고치는 것을 화면을 나누지 않고 하나의 채팅 흐름으로 통합. "고추기름을 사서 넣을지
    직접 만들지"처럼 애매한 부분은 **AI가 먼저 되물음**(자유 텍스트 응답과 구조화 제안을 한 응답에서
    섞어야 해서 `output_config.format` 대신 `propose_recipe` 커스텀 툴/함수를 호출하는 방식 사용).
    이 대화는 **실시간 웹 검색이 아니라 AI의 학습 지식 기반 구조화**이며, 사용자가 "웹 검색으로 참고하기"
    체크박스를 켜면 Claude는 `web_search`, Gemini는 `googleSearch`를 추가로 사용해 실제 표준 레시피를
    찾아 참고함(끄면 학습 지식만 사용, 검색 켜면 응답이 몇 초 더 걸림 — 개인 사용 기준 비용은 무시 가능한
    수준). Gemini 3 계열은 커스텀 함수+내장 툴을 같이 쓰려면 `toolConfig.includeServerSideToolInvocations:
    true`가 필요함(안 켜면 400 에러).
  - AI가 레시피를 제안할 때 **태그/재료 카테고리도 기존 목록 중에서 최대한 재사용해 자동 선택**하고,
    마땅한 게 없으면 새로 만들면서 대화 답변으로 짧게 안내함(`tagNames`/`categoryName` 필드, 기존
    태그·카테고리·재료 이름 목록을 프롬프트에 함께 전달 — `src/lib/aiChat.ts`의 `buildExistingContextNote`)
  - AI가 제안(propose_recipe)해도 **바로 폼에 반영되지 않고**, 지금 폼 상태와 비교한 변경 요약(재료
    추가/제거/수량 변경, 태그 변경 등 — `src/lib/recipeDiff.ts`, 추가=초록/제거=빨강/변경=강조색으로
    색 구분)을 먼저 보여준 뒤 "이대로 반영하기 / 무시하기"로 사용자가 확정. 채팅 입력창은 여러 줄 입력
    가능(Enter=전송, Shift+Enter=줄바꿈)
  - AI 적용(채팅 확정 또는 유튜브 변환)으로 폼이 바뀌기 직전 상태를 스택에 쌓아두고 **"AI 반영 이전으로
    되돌리기"** 버튼으로 여러 단계 되돌릴 수 있음(`RecipeEditor.tsx`의 `undoStack`)
  - **기존 레시피 수정 시 현재 내용을 대화에 자동으로 알려줌** — 편집 화면을 열면 대화가 비어있어도 지금
    폼에 있는 재료/조리순서/태그를 시스템 프롬프트에 포함시켜서(`buildCurrentRecipeNote`), "이 레시피를 더
    맵게 해줘" 같은 첫 메시지만으로 바로 수정 제안이 나옴. 채팅 패널 상단에 "지금 폼에 있는 '~' 내용을
    참고해서 대화해요"를 접기/펼치기(`<details>`)로 표시해 실제 참고 내용(재료·조리순서·태그 전체)을
    펼쳐볼 수 있음
  - 자주 쓰는 수정 요청("더 맵게", "재료 줄이기", "1인분으로")을 원클릭으로 보낼 수 있는 프리셋 칩 제공
    (기존 레시피 편집 중일 때만 노출)
  - **유튜브 링크로도 변환 가능**(별도 섹션, 채팅과는 분리) — 실제 영상 자막을 자동으로 추출해 사용함
    (`api/youtube-transcript.ts` 서버리스 함수, 한국어 자막 우선 → 영어 → 자동생성 자막 순 폴백). 로딩
    중에는 "자막 추출 중..." → "레시피 분석 중..."으로 단계가 표시됨. 자막이 아예 없는 영상은 지원 범위
    밖이라 명확한 안내 메시지와 함께 중단되고(대화창/직접 붙여넣기로 유도), 비공개·삭제된 영상은 별도
    메시지로 구분함. 결과는 항상 편집 폼에 채워서 사용자가 검토 후 저장(자동 저장 안 함), 추출이
    불확실하면 경고 문구 표시. 자세한 한계는 "유튜브 자막 추출 관련 TODO" 참고
- 레시피 수정 가능(상세 화면에서 편집 진입)
- **인분 조절 기능** (필수): 레시피 상세 화면에서 인분을 바꾸면 모든 재료 수량이 비례해서 자동 재계산됨
- **레시피 태그/카테고리**: 스타일 태그(크림류, 토마토류, 고기요리, 국물요리 등)를 레시피에
  달 수 있고, 태그를 새로 만들거나 편집/삭제하는 관리 액션도 별도로 존재
- **재료 단위**: 자유 텍스트가 아니라 자주 쓰는 단위(g, kg, ml, L, 개, 큰술, 작은술, 컵, 공기, 팩, 봉지,
  조각, 알, 단, 약간) 선택형 + "직접입력" 예외 처리 (`src/data/units.ts`)
- **조리 단계 타이머**: 초 단위 직접 계산 대신 분/초 두 칸 입력으로 편집(내부 저장은 여전히 초 단위 `timerSeconds`)
- **조리 단계 순서 변경**: 각 단계 카드에 "▲/▼" 버튼으로 배열 내 순서를 바꿀 수 있음(드래그 앤 드롭 대신
  단순 인덱스 교체 방식 — 모바일 터치에서 더 안정적)
- **조리 단계별 이미지**(AI 생성 또는 직접 업로드): 레시피 편집 화면의 각 조리 단계 카드에서 "🎨 이미지
  생성"(Gemini 전용, 온디맨드, 자동 생성 안 함) 또는 "📁 사진 업로드"(제공자 무관, 직접 찍은 사진으로 즉시
  교체)로 설정. 레시피 상세 화면(`RecipeDetailPage.tsx`)에서도 이미지가 단계 카드 상단에 표시됨. 저장
  방식은 "이미지 저장(IndexedDB)" 항목 참고
- **알러지 필터**: 레시피 자체에 알러지 태그를 수동으로 다는 대신, 레시피가 사용하는 재료들의 allergens
  값을 모아서 **자동으로 계산**됨. 레시피 목록 화면에서 "마늘 제외" 등 필터 가능(재료 기반 파생 속성)
- **장보기 유도**: 레시피 목록/상세 화면에 "🛒 장보기에 담기" 버튼이 있어, 누르면 장보기 선택 목록(공유
  저장소, `src/data/shoppingSelection.ts`)에 추가됨 — 장보기 탭으로 이동해도 선택이 유지됨

### 3. 장보기 리스트 화면
- 더 이상 별도의 "메인" 화면이 아니라, 레시피에서 "담기"로 유도되어 도착하는 화면
- 담긴 레시피들에 필요한 재료를 자동으로 집계 (재료ID+단위 기준 합산)
- 필터: 전체 / 구매 필요 / 보유
- 재료별 "실제 필요량"과 "장 볼 때 사야 할 추천 단위"는 별개 값으로 관리
- **영수증으로 재료 업데이트**(17차 확장): "📷 영수증으로 재료 업데이트" 버튼으로 영수증 사진을
  찍으면 AI가 품목을 인식해 확인 화면을 거쳐 재료 보유/추가에 반영됨. 자세한 내용은 위 "영수증
  스캔 재료 등록" 항목 참고.

### 4. 데이터 백업 — 기능 제거됨(13차 확장)
`localStorage` 단독 시절(Phase 1~2) "브라우저 데이터 삭제 시 유실 위험"을 대비한 JSON 내보내기/
가져오기 기능(`src/data/backup.ts`, 설정 화면 "데이터 백업" 섹션)이 있었으나, Supabase DB 전환
완료 후에는 그 전제 자체가 사라져서(데이터가 브라우저가 아니라 Supabase에 있음) 삭제함. "가져오기"
(복원)가 household 전체 데이터를 통째로 덮어쓰는 방식이라 다인원 공유 환경에서는 오히려 위험도가
더 큰 기능이었던 것도 삭제 이유 중 하나.

## 데이터 모델
Recipe/Ingredient/Category/Tag/MealPlan/ShoppingSelection/CookingLog 등 전체 타입 정의는
`src/data/types.ts` 참고(코드가 최신 소스). 코드만 봐서는 알기 어려운 것들:
- `allergens`는 레시피에 저장하지 않음 — `ingredients`를 통해 항상 파생(computed) 계산됨.
- `imageId`/`finalImageId`는 실제 이미지 데이터가 아니라 Supabase Storage 경로 참조
  (`src/data/imageStore.ts`).
- `PantryStatus`는 별도 저장소가 아니라 `Ingredient.owned`에서 파생되는 뷰 — DB에 따로 저장 안 됨.
- `content` jsonb에는 `sourceType`/`sourceNote`/`sourceRcpSeq`/`seedBatchId`가 들어갈 수 있음 —
  정식 필드가 아니라 공공데이터 시딩 스크립트(`scripts/seed-recipes-from-public-data.ts`)만
  기록하는 부가 정보(자세한 내용은 `docs/architecture.md`의 "공공데이터 기반 대량 레시피 시딩" 참고).
- `MenuSet`(손님초대모드용)은 아직 구현하지 않고 타입 설계만 해둔 스텁.
