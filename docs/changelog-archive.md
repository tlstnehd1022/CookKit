# CookKit 지난 확장 이력 (2 ~ 21차)

`CLAUDE.md`의 "진행 상황"에서 분리된 오래된 확장 이력입니다(22차 이후, 즉 최근 작업은
`CLAUDE.md`에 그대로 남아있음). **이 파일은 세션 시작 시 자동으로 로드되지 않습니다** — 아래
목록에서 지금 다루는 기능과 관련된 회차가 보이면 이 파일을 직접 Read할 것. 각 항목의 자세한
설계 배경은 `docs/architecture.md`(주제별)에도 나눠져 있을 수 있음.

---

- **2차 확장 완료**: 로그인 화면, 레시피 중심 네비게이션, 유튜브 링크 변환, 재료 카테고리 동적화
- **3차 확장 완료**: Google Gemini를 두 번째 AI 제공자로 추가(무료 쿼터 대안), 레시피 추가 화면 개선
  (재료 단위 선택형, 타이머 분/초 입력, 대화형 AI 레시피 편집 — 아래 "핵심 기능 요구사항"에 최신 상태 반영됨)
- **4차 확장 완료**: 유튜브 자막 자동 추출 — CORS 문제로 이 기능 하나만 예외적으로 최소 서버리스 함수를
  도입함(자세한 내용은 아래 "서버 도입(1호)" 항목과 "유튜브 자막 추출 관련 TODO" 참고)
- **5차 확장 완료**: 전체 디자인을 "따뜻한 아날로그 + Soft UI" 톤으로 통일 + 다크모드 토글, 재료별 개인
  선호 설정(선호 단위/방식), 조리 단계별 AI 이미지 생성(Gemini 전용, IndexedDB 저장 — 아래 "이미지 저장
  (IndexedDB)" 항목 참고)
- **6차 확장 진행 중 — Supabase(DB) 전환**: localStorage 단독 구조를 Supabase(Postgres + Auth)로
  옮기는 작업. **스키마 설계(Phase 1)/구글 로그인+가구 온보딩(Phase 2)/재료·레시피·장보기 데이터
  레이어 전환(Phase 3)까지 완료**, 실사용 테스트(로그인/가구 생성)도 확인함. 남은 건 API 키 암호화
  저장(Phase 4)뿐. 자세한 내용은 아래 "DB 전환(Supabase)" 항목 참고
- **7차 확장 완료 — 레시피 관리 화면 모바일 개편**: "가족들이 폰으로 오늘 뭐 해먹지 훑어보기 좋게"가
  목표. 2열 그리드 카드 뷰(기본) + 리스트 뷰 토글, 검색(이름+재료, 300ms 디바운스), 필터를 가로 스크롤
  한 줄로 통합(태그+알러지 제외+"🧺 보유 재료로 가능한 것만"), 정렬(최근 추가순/이름순), 빈 상태 처리.
  자세한 내용은 아래 "레시피 관리 화면(모바일 개편)" 항목 참고
- **8차 확장 완료 — 카테고리 중복 버그 수정 + 태그 국가/스타일 축 + 난이도/조리시간 자동 판단**: 재료
  카테고리 그루핑 버그의 진짜 원인(중복 생성 레이스 컨디션)을 찾아 수정하고 정리 스크립트 추가(위
  "버그(수정 완료) — 카테고리 중복 생성 레이스 컨디션" 참고). Tag에 `cuisine` 타입 추가(한식/양식 등,
  스타일/카테고리 태그와 별개 축, 레시피 편집 화면에서 구분된 섹션으로 다중 선택). Recipe에
  `difficulty`/`difficultyReason`/`estimatedMinutes` 필드 추가 — 규칙 기반 자동 계산(아래 "난이도/
  조리시간 자동 판단" 항목 참고), AI 대화형 생성 시에도 propose_recipe가 난이도를 함께 판단해서 채움.
- **9차 확장 완료 — 이미지 저장 Supabase Storage 전환 + 완성 사진**: 조리 단계 이미지 저장소를 기기
  로컬 IndexedDB에서 Supabase Storage(household 공유)로 옮겨서 household 구성원이 다른 기기에서도
  같은 이미지를 볼 수 있게 함. 완성 요리 사진(`Recipe.finalImageId`) AI 생성/업로드 기능 추가, 조리
  단계 이미지와 완성 사진이 같은 화풍 가이드(`IMAGE_STYLE_GUIDE`)를 공유하도록 통일. 자세한 내용은
  아래 "이미지 저장(Supabase Storage)" 항목 참고.
- **10차 확장 완료 — 다른 가구 공개 레시피 둘러보기/복사 + 레시피 공개범위 3단계**: RLS 보완 + "둘러보기"
  화면 + "내 레시피로 복사하기"(재료/태그/난이도/조리시간/이미지까지 완전 복제) + 레시피 편집 화면의
  공개범위 선택(개인 소유/가구 공유(기본)/전체 공개, `is_public` boolean을 대체)까지 완료. 공개 레시피
  좋아요(하트) 기능도 추가. 자세한 내용은 아래 "레시피 탐색/복사" 항목 참고.
- **11차 확장 완료 — 유튜브 변환 시 완성 사진 자동 제안**: 유튜브 링크로 레시피 변환할 때 영상 썸네일을
  완성 사진(`finalImageId`) 후보로 미리보기+체크박스로 제안, 반영 시 우리 Storage로 실제 복사해서
  저장. CORS 우회를 위해 서버리스 함수를 하나 더 추가(`api/youtube-thumbnail.ts`) — 자세한 내용은 위
  "서버 도입(2호)" 항목 참고.
- **12차 확장 완료 — 레시피 작성자 표시 + 프로필 닉네임/사진 + household 이름 짓기 가이드**: 설정
  화면에 닉네임(`profiles.display_name`) 변경 + household 이름 변경 UI 추가(둘 다 RLS는 이미
  본인/가구원 수정을 허용하고 있어서 프론트만 필요했음), "OO님의 레시피" 작성자 표시를 레시피
  카드/상세 화면(우리집+둘러보기 전부)에 추가, 구글 프로필 사진(`profiles.avatar_url`)도 같이
  저장해 작성자 표시 옆에 작은 아이콘으로 노출. household 생성 화면과 설정 화면 양쪽에 이름
  짓기 가이드 문구("우리집"류의 특정인 기준 호칭보다 "김영희네"처럼 누가 봐도 자연스러운 이름
  추천) 추가. 자세한 내용은 아래 "작성자 표시/프로필" 항목 참고.
- **13차 확장 완료 — API 키 Vault 전환(DB 전환 Phase 4)**: Anthropic/Gemini API 키를 브라우저
  localStorage 평문 저장에서 Supabase Vault 암호화 저장으로, 실제 AI 호출(대화/이미지 생성/유튜브
  자막→레시피 추출)도 브라우저 직접 호출에서 서버리스 함수 경유로 전환. 설정 화면의 키 입력 UI도
  마스킹 표시 + 인라인 편집 방식으로 개편, 기존에 localStorage에 남아있던 평문 키를 서버로 옮기는
  1회성 마이그레이션 배너도 추가. 실사용 중 YouTube Data API 키도 같은 방식으로 포함시켰고
  (원래는 범위 밖이었으나 요청으로 추가), 더 이상 의미 없어진 "데이터 백업"(JSON 내보내기/
  가져오기) 기능도 이번에 함께 제거. 자세한 내용은 아래 "API 키 Vault 전환" 항목 참고.
- **14차 확장 완료 — 재료 유통기한 관리 + PWA/웹 푸시 알림**: 재료에 유통기한(선택) 필드를
  추가하고 목록 화면에 임박/경과 배지 표시(1-2번, 완료·자체 검증 후 커밋). 이어서 PWA 설치
  지원(manifest+서비스워커, vite-plugin-pwa)과 유통기한 임박 시 매일 1회 웹 푸시 알림(Vercel
  Cron)까지 구현(3-4번), 실사용 테스트로 발송·설치까지 확인함. 알림을 클릭하면 재료 탭으로
  이동하면서 그 알림을 유발한 재료로 스크롤+잠깐 배경 강조(flash)되는 것까지 추가로 구현해
  확인 완료. iOS "홈 화면에 추가" 안내 문구도 코드는 완성해뒀지만 iOS 실기기 테스트는 나중으로
  미룸(안드로이드만 우선 확인). 영수증 촬영 자동 인식은 이번 범위에서 제외(별도 진행 예정).
  자세한 내용은 아래 "유통기한 관리 + PWA/웹 푸시 알림" 항목 참고.
- **15차 확장 완료 — 공공데이터 기반 대량 레시피 시딩(200개)**: 원래는 유튜브 영상 200개를
  AI로 분석해 레시피를 대량 생성할 계획이었으나 토큰 비용 문제로 중단하고, 식품의약품안전처
  공공 레시피 API로 전환해 200개를 가져와 이미 등록 완료. 자세한 내용은 아래 "공공데이터 기반
  대량 레시피 시딩" 항목 참고.
- **16차 확장 완료 — 레시피 목록 넷플릭스식 가로 스크롤 행 구조**: 검색/필터가 없는 기본 진입
  화면을 "🧺 보유 재료로 가능", "🆕 최근 추가됨", cuisine/style 태그별 가로 스크롤 행들로 개편(레시피가
  많아져도 첫 진입 시 세로 스크롤 길이가 짧게 유지되도록). "우리집 레시피"/"둘러보기" 두 화면 모두
  같은 공통 컴포넌트(`RecipeRowSection`/`RecipeCategoryDetailPage`)를 재사용하고, 둘러보기 화면은
  공공데이터 시드(시스템 계정 소유) 레시피를 일반 사용자 레시피와 분리해 접기/펼치기 섹션("🍳 CookKit
  추천 레시피")으로 뺐음. 검색어나 필터를 걸면 기존 그리드/리스트 결과 화면으로 그대로 전환됨(행
  구조는 순수 탐색용). 자세한 내용은 아래 "레시피 가로 스크롤 행 구조" 항목 참고.
- **17차 확장 완료 — 영수증 스캔 재료 등록 + 요리 완료 기록/재료 자동 차감**: 두 기능 모두
  AI/자동 처리 결과가 화면에 바로 반영되지 않고 항상 사용자 확인을 거친 뒤에만 저장되도록
  구현(자동 저장 금지 원칙). (1) 장보기 화면에 영수증 촬영/선택 버튼 추가 — Gemini 비전으로
  품목을 인식(`api/ai-receipt.ts`, 기존 Vault 경유 서버리스 함수 패턴 재사용)한 뒤 체크박스
  확인 화면(`ReceiptScanModal.tsx`)에서 이름/수량/카테고리를 수정하고 선택한 항목만 반영.
  (2) 레시피 상세 화면에 "🍳 오늘 만들었어요" 버튼 추가 — 재료 체크리스트 확인 모달
  (`CookingLogModal.tsx`)에서 확정한 재료만 보유 해제(owned=false)하고 `cooking_log`
  테이블(household 공유, 0018 마이그레이션)에 기록을 남김. 레시피 목록 정렬에 "자주 해먹은
  순"도 추가(이전부터 SortMode에 자리만 마련해뒀던 것을 실제로 채움). 자세한 내용은 아래
  "영수증 스캔 재료 등록"/"요리 완료 기록(CookingLog)" 항목 참고.
- **18차 확장 완료 — 요리 모드(핸즈프리 안내 + 음성 명령)**: 레시피 상세 화면에 "🍳 요리
  시작하기" 버튼 → 전체화면 큰 글씨 단계별 안내 모드(`CookingModePage.tsx`). Wake Lock으로
  화면 꺼짐 방지, 각 단계 진입 시 TTS로 안내, 마이크는 처음 한 번만 켜면(브라우저 정책상 최초
  탭 필요) 명령마다 다시 누르지 않고 계속 듣는 핸즈프리 방식. 화면 탭(이전/다음/타이머 시작·
  멈춤·초기화/종료)은 음성 지원 여부와 무관하게 항상 함께 제공(iOS Safari가 PWA standalone
  상태에서 음성 인식 자체를 지원 안 하는 알려진 문제가 있어서 그 경우 화면 탭이 유일한 수단).
  명령어는 일상 대화 속 오작동을 막기 위해 2어절 이상 조합만 인식. 완료 시 기존 "오늘
  만들었어요"(CookingLogModal) 흐름으로 자연스럽게 연결됨. 자세한 내용은 아래 "요리 모드(핸즈
  프리 안내 + 음성 명령)" 항목 참고.
- **19차 확장 완료 — 요리 모드 실제 소요시간 기록 + 복합 요리(여러 레시피 동시 진행)**: (A) 요리
  모드가 단계별 준비/조리 시간을 실측해서 기록하고, 레시피에 설정된 타이머 값과 꾸준히 다르면
  조정을 제안(`TimingAdjustmentModal`) — 완료 직후가 주 진입점, 레시피 상세의 "⏱ 조정 제안"
  배지가 보조 진입점. (B) 레시피 2~3개를 골라 동시에 진행하는 복합 요리 — 원본 레시피 내용은
  전혀 건드리지 않고 규칙 기반으로 "순서"만 배치(손질 단계 먼저, 타이머 긴 조리 단계부터), 미리
  보기 화면에서 드래그로 직접 재정렬 가능(이 프로젝트 첫 @dnd-kit 도입), 진행 화면은 여러
  타이머가 독립적으로 동시에 돌고 완료 시 레시피별로 각각 기록을 남김. 자세한 내용은 아래
  "요리 모드 실제 소요시간 기록"/"복합 요리(여러 레시피 동시 진행)" 항목 참고.
- **20차 확장 완료 — 실사용 피드백 12건 반영(버그 3 + 발견성 1 + 기능 5 + UI 2)**: 친구에게
  받은 실사용 피드백을 우선순위(버그 → 발견성 → 기능 → UI) 순으로 반영.
  - **버그**: (1) 레시피 상세에서 인분을 조절해도 재료 섹션 라벨이 원래 인분수로 고정되던 버그
    수정(`recipe.servingsBase` 대신 조절 중인 `servings` state를 라벨에 연동).
    (2) AI 채팅으로 조리순서만 바꾸거나 특정 단계에 타이머를 자연어로 요청해도("A 먼저 하고 B",
    "2단계에 5분 타이머") "큰 변화 없어요"로 응답하던 문제 — 원인은 두 곳이었음: `recipeDiff.ts`가
    `steps.length`만 비교해서 순서/내용 변경 자체는 diff로 안 잡혔던 것(단계별 title/content/
    timerSeconds를 위치별로 비교하도록 수정), `RECIPE_CHAT_SYSTEM_PROMPT`에 "순서 변경도 유효한
    수정", "시간 언급 시 timerSeconds 채우기" 규칙이 없었던 것(둘 다 프롬프트에 규칙 추가). (3) 신규
    계정 둘러보기 화면에 다른 사용자 공개 레시피가 안 보인다는 피드백 — RLS(`recipes_select_visibility`),
    `fetchPublicRecipes`의 가구원 제외 필터, `fetchHouseholdMemberIds`를 각각 정적 분석했으나 신규
    계정이 정상 온보딩(household 생성 완료)을 거친 뒤라면 재현되는 명확한 원인을 찾지 못함(피드백에
    적힌 "AI 키 미설정 때문"은 코드상 무관함을 확인 — 둘러보기는 DB 조회만 함). 유일하게 발견한
    잠재적 결함은 `DiscoverRecipesPage.tsx`의 데이터 로드 `useEffect`가 `[user?.id]`에만 의존해서
    `householdId`가 늦게 준비되는 타이밍이면 재조회가 안 될 수 있다는 것인데, 이게 실제 재현
    원인인지는 미확인 — **이번엔 수정하지 않고 원인 불명 상태로 남겨둠**(사용자에게 원인 파악
    결과를 먼저 보고하기로 한 지시에 따름). 재현되면 (a) 신규 계정이 정말 household 생성 직후
    바로 둘러보기로 진입했는지, (b) 그 시점에 다른 household의 `visibility='public'` 레시피가
    실제로 DB에 존재했는지(둘러보기는 "다른 household 소유"만 보여주므로 테스트 계정 혼자
    실험하면 원래도 0건이 정상) 먼저 확인할 것.
  - **발견성**: 알러지 필터 기능 자체는 있었지만(재료 allergens → 레시피 자동 파생 → 목록 필터)
    발견이 안 되고 있었음 — `HouseholdOnboarding.tsx`에 household 생성 직후 "알러지나 못 먹는
    재료가 있나요?" 단계 추가(건너뛰기 가능, 입력하면 "기타" 카테고리에 allergens 설정된 재료로
    자동 등록), `RecipesPage.tsx`의 알러지 제외 필터를 일반 필터 칩 줄에서 분리해 별도
    "⚠️ 알러지 제외" 섹션으로 승격.
  - **기능**: (1) AI 반영 직후마다 이미지 생성을 물어보던 것을 저장 시점 1회로 변경(비용 절감,
    이미 이미지가 있으면 재질문 안 함) — `RecipeEditor.tsx`의 `handleSave()`가 저장 직전에
    이미지 생성 여부를 확인하고, 생성된 최신 steps/finalImageId를 `useRef`로 읽어서(클로저
    스테일 방지) 최종 저장 payload에 반영. (2) 설정 화면에 프로필 사진 직접 업로드 추가(`avatars`
    Storage 버킷 신설, 0020 마이그레이션, 공개 버킷 — 작성자 아이콘이 다른 household 화면에서도
    보여야 해서), "구글 사진으로 되돌리기"는 `SessionUser.googleAvatarUrl`(OAuth user_metadata에서
    파생)을 다시 적용하는 방식. (3) 태그 섹션 이름을 더 직관적으로 변경("스타일 태그"→"요리
    스타일", "국가/스타일"→"국가/장르")하고 각 섹션에 한 줄 설명 추가(레시피 편집 화면 + 태그
    관리 화면 모두). (4) "오늘 만들었어요" 완료 후 같은 모달이 "완료 화면"으로 전환되어 직접
    찍은 사진 업로드(`imageStore`) + 대표 이미지(finalImageId) 설정 여부 선택(선택 사항) +
    "맛이 별로였어요 → 레시피 수정하기" 버튼(편집 화면 바로 진입)을 제공 — 요리 모드를 거쳐 온
    경우의 시간 조정 제안은 완료 화면과 이중으로 뜨지 않도록 자동 팝업을 없애고 기존 "⏱ 조정
    제안 있음" 칩으로만 노출하도록 단순화. (5) 레시피 편집 화면 저장 버튼을 화면 하단 sticky로
    고정.
  - **UI**: (1) 재료 목록을 "⏰ 유통기한 임박/경과"(기존, 최상단) / "✅ 있어요" / "🛒 없어요"
    3구간으로 시각 분리(각 구간 안에서는 기존 카테고리별 접기/펼치기 그루핑 유지). (2) 레시피
    상세 화면의 인분 조절 스테퍼를 별도 섹션에서 "재료" 섹션 제목 옆으로 옮겨 항상 재료 목록과
    함께 보이게 함.
  - **실기기/수동 테스트 필요(자동 빌드 검증만으로는 확인 불가)**: AI 채팅 조리순서/타이머 자연어
    반영(BUG-02, 실제 API 키 필요), 신규 계정 둘러보기 재현 여부(BUG-03, 원인 불명 상태), 프로필
    사진 업로드 실제 동작(0020 마이그레이션 실행 완료, 실사용 테스트는 아직), 알러지 온보딩 단계
    실제 가입 플로우, 이미지 생성 저장 시점 변경, 태그 발견성/설명 문구가 실제로 이해하기 쉬운지,
    "오늘 만들었어요" 완료 화면 사진 업로드/대표이미지 반영, 3구간 재료 목록/인분 조절 위치가
    실기기 화면에서 자연스러운지. 회귀 테스트(레시피 저장/수정/삭제, 유튜브 변환, 장보기 담기
    유지, 다크모드 토글)도 사용자 확인 필요.
- **21차 확장 완료 — 재료 관리 화면 "냉장고" 컨셉 개편(DIS-06)**: 친구 피드백 "재료 관리 flow가
  추상적이라 뭘 해야 할지 모르겠다"를 반영해 "채우기"라는 명확한 동작 중심으로 화면을 재구성.
  - **이름/구조**: 하단 탭 라벨을 "재료"→"냉장고"(🧊)로 변경(내부 컴포넌트/함수/변수명인
    `IngredientsPage` 등은 그대로 유지 — 화면에 노출되는 라벨만 교체). 화면 최상단에 "📷 영수증으로
    채우기"/"➕ 직접 추가" 두 액션을 나란히 배치, 둘 다 모달로 뜨고 완료돼도 탭 전환 없이 그 자리에서
    아래 목록에 바로 반영됨(기존 반응형 store 구독 구조 덕분에 추가 작업 없이 자연히 만족).
    `ReceiptScanModal.tsx`는 원래 `features/shopping-list/`에 있었는데 재료 관리 성격이 더 맞아서
    `features/ingredients/`로 옮기고, 장보기 화면의 기존 진입점은 그대로 남겨 같은 모달을 가리키게
    함(두 곳 다 같은 기능). "➕ 직접 추가"(`AddIngredientModal`)는 이제 "채우기" 흐름의 일부라
    기본값을 `owned:false`에서 `owned:true`로 변경 — 예전엔 "재료 정의만 등록"하는 의미였다면
    이제는 "지금 채워 넣는다"는 의미로 재정의됨.
  - **채움 이력 추적**(`0021_ingredient_fill_tracking.sql`): `ingredients.last_filled_at`(최근
    채움 시각, 냉장고 화면 "n일 전 채움" 배지용 — 단일 값이라 빠르게 조회) +
    `ingredient_fill_log`(household 공유, 채워질 때마다 한 줄씩 쌓는 이력 — 장보기 선제 제안이
    "최근 90일 몇 번 채웠는지/평균 재구매 간격"을 계산할 근거). `useIngredients()`에
    `markIngredientFilled(item)`을 추가해 "채우는 의도가 분명한" 호출(보유 토글 켜기 — `usePantryStatus.
    setOwned(id, true)`, 영수증 반영, 직접 추가)에서만 쓰고, 일반 `saveIngredient`(알러지/선호
    설정 편집 등 owned 값을 그대로 들고 다니는 무관한 저장)는 건드리지 않아 의도치 않은 이력 오염을
    막음. **이미 owned=true인 상태에서 다시 채워도(예: 아직 남았는데 미리 사둔 경우) 유효한 구매
    이력으로 보고 매번 갱신/기록**하도록 명시적으로 요청받아, "채움" 판정을 `owned` 상태 변화가
    아니라 "채우는 액션이 호출됐는가"로 정의함(레거시 재료는 `lastFilledAt`이 없어 배지 자체가 안 뜸).
  - **선제 제안 로직**(`src/lib/refillSuggestions.ts`, `src/data/ingredientFillLog.ts`): 명세에는
    "lastFilledAt 갱신 이력을 보고 재구매 주기를 추정"이라고 돼 있었지만, 단일 스칼라 필드
    (`lastFilledAt`)만으로는 "최근 90일 몇 번 채웠는지"를 셀 수 없어서(마지막 한 시점만 앎) 이번에
    `ingredient_fill_log` 이력 테이블을 별도로 둔 것 — `lastFilledAt`은 화면 배지용 캐시,
    실제 빈도/간격 계산은 이 로그 테이블 기준. "평소 재구매 주기의 1.5배 지나면 제안"의 "재구매
    주기"는 로그에 쌓인 채움 시각들 사이의 평균 간격(`avgIntervalDays`)으로 계산, 최근 90일 채움
    3회 미만이면 판단 근거 부족으로 제안하지 않음(명세대로). 계산이 무거울 수 있어 장보기 화면
    진입 시 1회만 조회(`fetchFillFrequencies`, `usePantryStatus`처럼 계속 구독하지 않는
    recipeLikes.ts/cookingLog.ts류 "1회 조회" 패턴). 제안 카드의 "장보기에 담기" 버튼은 명세
    원문 그대로의 문구였지만, 이 앱의 장보기 리스트는 "레시피 선택 → 재료 자동 집계" 구조라
    개별 재료를 직접 담는 개념 자체가 없어서 — 대신 "✅ 채웠어요" 버튼으로 바로 보유 처리
    (`markIngredientFilled`와 동일 경로)하는 쪽으로 판단해서 진행. 데이터가 쌓여야 의미 있는
    기능이라 실제 제안이 뜨는지는 이번 배치에서 확인하지 못함 — 몇 주간 실사용해서 재료별 채움
    이력이 쌓인 뒤에 확인 필요.
